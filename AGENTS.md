# 倾境壁纸工程约定

## 执行方式

- 当前 1.0 已进入成熟维护阶段，产品、需求、设计、交接、验收和项目看板文档全部冻结；除非用户明确要求，不再新增、补写或更新 1.0 说明文档，也不再为了普通改动维护 `PM-002` 状态。
- 用户明确指出页面、字段、接口或代码位置的小改动，只读取直接相关的代码和测试后实施；不得在开始前扫描项目文档、遍历历史方案或扩大调研范围。
- 只有用户明确指定某份文档，或者当前任务本身是部署、生产数据操作时，才按需读取对应文档；部署和数据操作统一以 `OPS-005` 为现行规则入口。
- OpenAPI、Flyway 迁移、Schema 和构建配置属于可执行工程契约，不属于本次文档冻结范围；代码涉及这些契约时必须同步修改并验证。
- 用户明确宣布开始 2.0 后，再建立一份新的、精简的 2.0 总文档和任务看板；只记录 2.0 当前事实，不复制 1.0 的历史过程文档。
- 每完成一个可独立验收的大流程，创建明确 Git 提交并同步到 `https://github.com/Jerry1254/wallpapersapp.git`。
- 提交前运行与改动直接相关的构建和测试，并检查密钥和运行时文件未进入 Git。

## 系统边界

- 正式 API 工程位于 `services/api-server`，数据库迁移使用 Flyway，契约位于 `contracts/openapi`。
- MySQL 是业务事实的唯一来源；Redis 只用于缓存、限流、短时票据和幂等协调。
- 首阶段资源使用本地文件存储 Adapter，运行时目录不进入 Git；业务代码不直接依赖文件系统路径。
- H5 已完成并冻结，后续不再维护，也不作为新需求、实现或验收依据。
- 管理后台只使用独立管理 API，不直接访问数据库或资源目录。
- 当前为单业务主体，不引入多租户或 RBAC。

## 环境与安全

- 日常开发默认使用本地 API、MySQL、Redis 和本地资源目录。
- 测试和生产环境必须使用独立数据、配置和凭据，不在环境间同步业务数据。
- 密码、Token、私钥、证书私钥和数据库密码不得进入 Git。
- 未经明确确认不执行生产发布、生产迁移或生产数据操作。
