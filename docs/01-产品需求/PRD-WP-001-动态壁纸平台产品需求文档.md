# 动态壁纸平台产品需求文档

## 一、概述

**文档信息**

| 版本号 | 日期 | 作者 |
|--------|------|------|
| V1.0.0 | 2026-09-10 | — |

### 1. 需求背景

- **目标用户现状**：用户在抖音直播间看到壁纸效果，付费后由客服私发兑换码，希望不注册账号即可获得并设置壁纸。
- **核心痛点**：普通图片下载不能直接获得分层视差效果，动态资源又容易出现格式不兼容、重复收费、换包丢失和资源链接泄露。
- **当前问题汇总**：

| 问题点 | 问题描述 | 当前影响 |
|--------|----------|----------|
| 动态效果依赖平台能力 | Android、iOS、HarmonyOS 的壁纸机制不同 | 同一文件不能直接覆盖三个平台 |
| 交付流程割裂 | 购买、收码、下载、设置分散在不同环节 | 用户操作成本高，客服难排障 |
| 无账号但要保留权益 | 用户不登录，仍需识别同一设备和已兑换内容 | 重装后的权益恢复和设备更换需要独立机制 |
| 兑换额度需要冻结 | 新生成兑换码的额度会调整，但历史码不能随默认值变化 | 必须保存每个批次和每个码的额度快照 |
| 数字资源容易被复制 | 固定下载地址、明文资源包和简单抓包会造成泄露 | 需要服务端授权、短时下载票据和包加密 |

### 2. 需求目标

- **业务目标**：
  - 完成“浏览—预览—兑换—下载—设置—再次下载”的 Android 闭环。
  - 让客服可批量生成兑换码，并准确查看每次兑换、设备归属和剩余额度。
  - 从第一版建立 Flutter 共享层与平台原生插件边界，保持 iOS、HarmonyOS 扩展能力。
- **成功标准**：

| 指标 | 当前状态 | 目标状态 |
|------|----------|----------|
| H5 原型 P0 流程覆盖 | 未建立 | 本文 8 项 P0 流程全部可操作并完成人工验收 |
| 兑换额度正确性 | 未实现 | 并发兑换不超发，重复请求不重复扣减 |
| 同设备重复下载 | 未实现 | 已获得壁纸不再要求兑换码 |
| Android 设置闭环 | 单套本地素材已验证 | 在线资源可完成校验、安装、系统预览和设置 |
| 历史码额度稳定性 | 未实现 | 修改默认额度不影响已生成兑换码 |
| 受保护资源地址 | 当前规范包含固定地址 | 公开接口不返回永久资源地址 |

### 3. 需求范围

- **包含**：
  - 移动 H5 原型：分类、列表、详情、预览、兑换、下载和“我的壁纸”。
  - 管理后台原型：分类、壁纸、平台资源、兑换码批次、兑换记录和设备支持。
  - Android 首发 App：Flutter 业务界面与 Android 原生动态壁纸插件。
  - Java API、MySQL、Redis、对象存储及统一 OpenAPI 契约。
  - 匿名设备注册、同设备权益恢复、资源授权下载和操作审计。
- **不包含**：
  - 在线支付和抖音订单对接。
  - 用户注册、手机号登录和用户账户中心。
  - iOS、HarmonyOS 功能实现和应用市场上架。
  - 对截图、Root、Hook、内存提取的绝对防复制承诺。

### 4. 参考资料

| 文档名称 | 链接/说明 |
|----------|-----------|
| Android 技术验证 | [安卓动态壁纸完整流程](../02-系统架构/安卓动态壁纸完整流程.md) |
| 壁纸资源包 | [壁纸资源包规范 v0.1](../03-数据设计/壁纸资源包规范-v0.1.md) |
| Android 官方能力 | [WallpaperService](https://developer.android.com/reference/android/service/wallpaper/WallpaperService)、[WallpaperManager](https://developer.android.com/reference/android/app/WallpaperManager) |
| iOS 扩展依据 | [Apple Live Photo 保存能力](https://developer.apple.com/documentation/avfoundation/capturing-and-saving-live-photos) |
| HarmonyOS 扩展依据 | [华为主题中心](https://developer.huawei.com/consumer/cn/doc/content/beginner-guide-0000001054200638/) |
| 技术架构 | [混合客户端与平台架构方案](../02-系统架构/ARC-001-混合客户端与平台架构方案.md) |

## 二、功能列表

| 模块 | 功能名称 | 功能说明 | 优先级 |
|------|----------|----------|--------|
| 用户端 | 壁纸分类与列表 | 按一级分类查看已发布壁纸并识别静态、4D 等类型 | P0 |
| 用户端 | 详情、动态预览与适配 | 查看效果、资源信息和当前设备可用性 | P0 |
| 用户端 | 兑换码与设备权益 | 用任意有效兑换码获得一张壁纸，同设备恢复权益 | P0 |
| 用户端 | 下载、安装与设置 | 安全下载资源并进入 Android 系统设置流程 | P0 |
| 管理端 | 管理员访问 | 管理员登录并进入受保护工作区 | P0 |
| 管理端 | 分类、壁纸与平台资源管理 | 编辑内容、上传资源版本、校验并发布 | P0 |
| 管理端 | 兑换码批次管理 | 批量生成、一次性交付、停用并查看额度 | P0 |
| 管理端 | 兑换记录与设备支持 | 查询核销明细、恢复设备和处理异常权益 | P0 |
| 用户端 | 搜索、收藏、排行榜与分享 | 提升内容发现效率 | P2 |
| 平台扩展 | iOS 与 HarmonyOS 适配实现 | 接入对应平台原生能力和商店流程 | P2 |
| 商业扩展 | 在线支付、自动发码与用户账号 | 建立自动交易和跨设备账户权益 | P2 |

> 优先级定义：P0 = MVP 必须上线 / P1 = 重要但可延后 / P2 = 可选优化

## 三、页面结构与导航设计

### 整体导航结构

    用户端：底部双 Tab
    ├─ 首页       → 浏览分类、推荐和壁纸列表
    └─ 我的       → 查看教程、当前设备已购壁纸和微信客服

    管理端：左侧导航
    ├─ 分类管理   → 维护一级分类和排序
    ├─ 壁纸管理   → 维护壁纸、平台资源、版本和发布状态
    ├─ 兑换码     → 生成批次、一次性导出和停用
    └─ 兑换记录   → 查询设备权益、兑换流水和恢复操作

### 用户端：首页

**页面职责：** 让用户快速找到可在当前设备使用的壁纸。

**页面元素（从上到下）：**

| 区域 | 内容 | 显示条件 |
|------|------|----------|
| 顶部栏 | 品牌、客服入口 | 始终显示 |
| 推荐区 | 运营配置的精选壁纸 | 有已发布推荐内容时 |
| 分类栏 | 风景、国风/禅意、原创角色等一级分类 | 始终显示 |
| 壁纸列表 | 封面、名称、类型标签 | 始终显示 |
| 分页加载 | 加载更多、到底提示 | 列表超过一页时 |

**本页面不展示：** 平台、权益状态、兑换码输入框、完整资源地址、管理状态和设备指纹。

### 用户端：我的

**页面职责：** 提供壁纸设计教程、当前设备已经获得的壁纸和微信客服入口。

**页面元素（从上到下）：**

| 区域 | 内容 | 显示条件 |
|------|------|----------|
| 壁纸设计教程 | 预览、制作和设置教程入口 | 始终显示，位于页面顶部 |
| 已获得列表 | 壁纸封面、类型和名称 | 有权益时 |
| 空状态 | 返回首页按钮 | 无权益时 |
| 微信客服 | 微信二维码、微信号复制、服务时间 | 始终显示 |

**本页面不展示：** 下载/设置状态、独立设置按钮、设备支持编号、完整兑换码、其他设备权益和管理员恢复操作。

### 管理端：分类管理

**页面职责：** 管理首页一级分类及其排序和启停状态。

**页面元素（从上到下）：**

| 区域 | 内容 | 显示条件 |
|------|------|----------|
| 操作栏 | 新建分类、状态筛选 | 始终显示 |
| 分类表格 | 名称、标识、排序、状态、壁纸数 | 始终显示 |
| 编辑抽屉 | 分类字段和保存操作 | 新建或编辑时 |

**本页面不展示：** 壁纸资源文件、兑换码明文和兑换流水。

### 管理端：壁纸管理

**页面职责：** 管理壁纸内容、平台变体、资源版本及发布状态。

**页面元素（从上到下）：**

| 区域 | 内容 | 显示条件 |
|------|------|----------|
| 筛选栏 | 名称、分类、类型、平台、状态 | 始终显示 |
| 壁纸表格 | 封面、名称、分类、可用平台、版本、状态 | 始终显示 |
| 编辑页 | 基础信息、平台变体、资源上传、预览和校验结果 | 新建或编辑时 |
| 发布对话框 | 校验摘要、版本和发布确认 | 点击发布时 |

**本页面不展示：** 用户付款信息和永久公开下载链接。

### 管理端：兑换码

**页面职责：** 按批次生成具有固定额度快照的兑换码并交付客服。

**页面元素（从上到下）：**

| 区域 | 内容 | 显示条件 |
|------|------|----------|
| 统计区 | 批次数、已生成、已用额度、剩余额度 | 始终显示 |
| 操作栏 | 生成批次、状态筛选 | 始终显示 |
| 批次表格 | 批次名、数量、每码额度、使用情况、状态、创建时间 | 始终显示 |
| 生成对话框 | 数量、每码额度、有效期 | 点击生成时 |
| 一次性交付 | CSV 下载与确认提示 | 批次生成成功后仅本次会话显示 |

**本页面不展示：** 历史兑换码完整明文和资源解密密钥。

### 管理端：兑换记录

**页面职责：** 查询每一笔额度消耗及设备权益，并处理恢复问题。

**页面元素（从上到下）：**

| 区域 | 内容 | 显示条件 |
|------|------|----------|
| 筛选栏 | 掩码兑换码、设备支持编号、壁纸、时间、结果 | 始终显示 |
| 记录表格 | 兑换时间、设备、壁纸、额度变化、结果、请求编号 | 始终显示 |
| 详情抽屉 | 兑换事实、设备状态、权益和审计记录 | 点击记录时 |
| 恢复对话框 | 目标设备支持编号、原因和影响范围 | 管理员执行恢复时 |

**本页面不展示：** 原始设备标识、兑换码完整明文、资源密钥和客户端令牌。

### 独立页面 / 弹窗

| 页面/弹窗名称 | 触发条件 | 与主导航的关系 |
|---------------|----------|----------------|
| 分类列表页 | 点击首页分类 | 覆盖主页面，可返回首页 |
| 壁纸详情页 | 点击壁纸卡片 | 覆盖主页面 |
| 兑换码弹窗 | 未获得壁纸时点击下载 | 覆盖详情页 |
| 下载与安装面板 | 兑换成功或已获得后点击下载 | 覆盖详情页 |
| Android 系统壁纸预览 | 点击设置壁纸 | 跳转系统页面，返回后刷新状态 |
| 管理员登录页 | 管理端无有效会话 | 替换管理导航 |
| 壁纸编辑页 | 新建或编辑壁纸 | 管理工作区独立页面 |

## 四、功能说明

### 功能 1：壁纸分类与列表

**功能描述**

用户可在首页和分类列表页浏览已发布壁纸，解决直播间用户拿到 App 后无法快速找到目标内容的问题。

**用户流程**

    打开首页
      ↓
    加载分类与推荐内容
      ├─ 成功 → 展示分类和壁纸卡片
      ├─ 数据为空 → 展示空状态和刷新按钮
      └─ 网络失败 → 保留缓存并允许重试
      ↓
    点击分类或壁纸
      ├─ 分类 → 进入分类列表
      └─ 壁纸 → 进入详情页

**状态机**

| 状态名称 | 进入条件 | 退出条件 | 用户可见表现 |
|----------|----------|----------|--------------|
| 加载中 | 首次进入或刷新 | 请求成功或失败 | 骨架屏 |
| 有内容 | 返回已发布内容 | 刷新或离开 | 分类和卡片 |
| 空内容 | 当前分类无内容 | 更换分类或刷新成功 | 空状态 |
| 缓存内容 | 网络失败且本地有缓存 | 联网刷新成功 | 旧内容及离线提示 |
| 加载失败 | 网络失败且无缓存 | 重试成功 | 错误状态 |

**字段规范**

本功能无用户输入字段，仅展示系统生成或已保存数据。

**文案规范**

| 场景 | 文案内容 |
|------|----------|
| 空状态 | 暂时没有可用壁纸 |
| 加载中 | 正在加载壁纸 |
| 操作成功 | 已更新 |
| 操作失败 | 加载失败，请重试 |
| 权限不足 | 当前内容暂不可用 |
| 网络异常 | 网络不可用，已展示最近内容 |

**异常情况处理**

| 异常场景 | 触发条件 | 处理方式 | 用户反馈 |
|----------|----------|----------|----------|
| 网络断开 | 列表请求失败 | 优先读取已过期缓存并标记 | 网络不可用，已展示最近内容 |
| 数据为空 | 无已发布内容 | 展示空状态 | 暂时没有可用壁纸 |
| 操作超时 | 20 秒无响应 | 取消请求并提供重试 | 加载超时，请重试 |
| 权限不足 | 内容已下架或渠道不可见 | 从列表移除并刷新 | 当前内容暂不可用 |

### 功能 2：详情、动态预览与适配

**功能描述**

用户可在壁纸详情页查看接近手机比例的完整预览。服务端仍按当前平台返回可用变体；界面通过主按钮状态处理设备不兼容，不额外展示平台和适配说明区。

**用户流程**

    进入壁纸详情
      ↓
    服务端返回当前平台可用变体
      ├─ 支持动态 → 允许进入动态预览
      ├─ 仅支持静态 → 展示静态效果和说明
      └─ 完全不支持 → 禁止下载并展示原因
      ↓
    启动预览
      ├─ 传感器可用 → 根据手机倾斜渲染
      ├─ 权限未授予 → 请求权限或使用拖动模拟
      └─ 性能不足 → 自动降级为低质量预览
      ↓
    未获得用户点击“试用 2 分钟”
      ├─ 打开 App 内全屏效果试用
      ├─ 显示 02:00 倒计时，可提前结束
      └─ 到期 → 自动关闭试用并清理临时资源，不创建设备权益

**状态机**

| 状态名称 | 进入条件 | 退出条件 | 用户可见表现 |
|----------|----------|----------|--------------|
| 检测中 | 打开详情 | 能力检测完成 | 检测提示 |
| 可动态使用 | 有匹配平台变体和能力 | 资源状态变化 | 预览、下载按钮可用 |
| 静态降级 | 只有静态变体或性能不足 | 用户返回 | 静态预览及说明 |
| 不兼容 | 无匹配变体 | 内容补齐或换设备 | 禁用下载 |
| 预览中 | 详情页加载动态预览 | 离开详情或发生错误 | 主预览区域内显示视差效果 |
| 预览失败 | 资源或渲染失败 | 重试成功 | 封面和错误提示 |
| 试用中 | 未获得用户点击试用 | 提前结束或倒计时归零 | 全屏效果及剩余时间 |
| 试用结束 | 用户结束或 120 秒到期 | 返回详情 | 提示试用已结束 |

**字段规范**

本功能无用户输入字段，仅展示系统生成或已保存数据。

**文案规范**

| 场景 | 文案内容 |
|------|----------|
| 空状态 | 暂无可预览资源 |
| 加载中 | 正在准备预览 |
| 操作成功 | 预览已就绪 |
| 操作失败 | 预览加载失败，请重试 |
| 试用入口 | 试用 2 分钟 |
| 试用结束 | 2 分钟试用已结束 |
| 权限不足 | 允许使用动作与方向传感器以体验倾斜效果 |
| 网络异常 | 网络异常，暂时使用封面预览 |

**异常情况处理**

| 异常场景 | 触发条件 | 处理方式 | 用户反馈 |
|----------|----------|----------|----------|
| 网络断开 | 预览资源未缓存 | 使用封面 | 网络异常，暂时使用封面预览 |
| 数据为空 | 平台变体缺失 | 禁止下载 | 当前设备暂不支持这张壁纸 |
| 操作超时 | 资源 20 秒未完成 | 取消并可重试 | 预览准备超时 |
| 权限不足 | 传感器权限被拒绝 | 使用手势模拟，不阻断浏览 | 可拖动屏幕查看效果 |

### 功能 3：兑换码与设备权益

**功能描述**

用户可在壁纸详情页输入任意有效兑换码，为当前设备获得一张壁纸；已获得内容在同设备重新安装后自动恢复。

**用户流程**

    未获得壁纸时点击下载
      ↓
    输入兑换码并提交
      ↓
    服务端执行幂等事务
      ├─ 当前设备已拥有 → 不扣额度，直接返回权益
      ├─ 码有效且有余额 → 扣减 1 个额度并创建设备权益
      ├─ 码无效/停用/过期 → 拒绝兑换
      ├─ 码额度已用完 → 拒绝兑换
      └─ 返回结果未知 → 按请求编号查询，不重复提交
      ↓
    兑换成功 → 进入下载流程

    App 重装后启动
      ↓
    重新注册匿名设备
      ├─ 识别为同一设备 → 恢复全部有效权益
      └─ 无法识别 → 展示设备支持编号并引导联系客服

**状态机**

| 状态名称 | 进入条件 | 退出条件 | 用户可见表现 |
|----------|----------|----------|--------------|
| 未获得 | 当前设备无权益 | 兑换成功 | 下载前显示兑换 |
| 校验中 | 提交兑换码 | 得到确定或未知结果 | 按钮禁用并显示进度 |
| 已获得 | 权益已存在或新建成功 | 权益被管理员撤销 | 可直接下载 |
| 码无效 | 格式错误或不存在 | 修改输入 | 输入框错误 |
| 码不可用 | 停用、过期或额度耗尽 | 更换兑换码 | 原因提示 |
| 结果待确认 | 请求超时但可能已提交 | 查询到最终结果 | 禁止重复扣减 |
| 恢复失败 | 无法识别原设备 | 客服恢复完成 | 显示支持编号 |

**字段规范**

| 字段名 | 类型 | 是否必填 | 长度限制 | 校验规则 | 错误提示 |
|--------|------|----------|----------|----------|----------|
| 兑换码 | 文本 | 是 | 20 位有效字符 | 忽略空格和连字符；转大写；只接受系统字符集 | 请输入正确的兑换码 |
| 幂等请求编号 | 系统生成文本 | 是 | 36 位 | 每次用户意图固定一个 UUID，重试复用 | 系统正在确认兑换结果 |
| 设备支持编号 | 系统生成文本 | 是 | 10 至 16 位 | 只用于客服定位，不作为认证秘密 | 无 |

**文案规范**

| 场景 | 文案内容 |
|------|----------|
| 空状态 | 输入客服发送的兑换码 |
| 加载中 | 正在兑换，请勿重复操作 |
| 操作成功 | 兑换成功，这张壁纸已绑定当前设备 |
| 操作失败 | 兑换失败，请检查兑换码 |
| 权限不足 | 该兑换码当前不可使用 |
| 网络异常 | 网络异常，正在确认兑换结果 |

**异常情况处理**

| 异常场景 | 触发条件 | 处理方式 | 用户反馈 |
|----------|----------|----------|----------|
| 网络断开 | 提交前无网络 | 不发起请求，保留输入 | 网络不可用，请稍后重试 |
| 数据为空 | 兑换码输入为空 | 阻止提交 | 请输入兑换码 |
| 操作超时 | 提交后 20 秒无结果 | 使用同一幂等编号查询结果 | 正在确认兑换结果 |
| 权限不足 | 码停用、过期或额度耗尽 | 不创建权益、不扣额度 | 显示服务端确定原因 |
| 并发超额 | 最后额度被其他请求占用 | 条件更新失败并回滚 | 兑换码额度已用完 |
| 重复兑换 | 当前设备已拥有同一壁纸 | 返回原权益，不扣额度 | 这张壁纸已经获得 |

### 功能 4：下载、安装与设置

**功能描述**

用户可在详情页或“我的壁纸”安全下载已获得内容，完成校验、安装和 Android 系统壁纸设置。

**用户流程**

    点击下载
      ↓
    服务端校验设备会话和壁纸权益
      ├─ 有权 → 返回短时下载票据
      └─ 无权 → 回到兑换流程
      ↓
    下载加密资源包
      ├─ 完成并校验通过 → 原子安装
      ├─ 中断 → 保留可恢复临时状态
      └─ 校验失败 → 删除临时文件并重新获取票据
      ↓
    点击设置壁纸
      ↓
    打开 Android 系统预览
      ├─ 用户确认 → 刷新为已设置
      ├─ 用户取消 → 保持已下载
      └─ 厂商不支持指定目标 → 显示系统可用选项

**状态机**

| 状态名称 | 进入条件 | 退出条件 | 用户可见表现 |
|----------|----------|----------|--------------|
| 未下载 | 已获得但本地无包 | 开始下载 | 下载按钮 |
| 获取授权 | 请求下载票据 | 允许或拒绝 | 准备下载 |
| 下载中 | 收到票据 | 完成、中断或失败 | 进度和取消 |
| 校验安装中 | 文件完成 | 校验成功或失败 | 安装提示 |
| 已下载 | 本地版本可用 | 设置或更新 | 设置按钮 |
| 有更新 | 服务端版本高于本地 | 更新完成 | 更新按钮 |
| 系统预览中 | 调起系统页面 | 用户返回 | App 进入后台 |
| 已设置 | 系统确认当前服务 | 更换壁纸 | 已设置标记 |
| 下载失败 | 网络或校验失败 | 重试成功 | 错误和重试 |

**字段规范**

本功能无用户输入字段，仅展示系统生成或已保存数据；设置目标由 Android 系统页面提供，App 不伪造系统确认结果。

**文案规范**

| 场景 | 文案内容 |
|------|----------|
| 空状态 | 当前设备还没有已获得壁纸 |
| 加载中 | 正在安全下载 |
| 操作成功 | 下载完成，可以设置壁纸 |
| 操作失败 | 下载失败，请重试 |
| 权限不足 | 请先兑换这张壁纸 |
| 网络异常 | 网络中断，稍后可以继续下载 |

**异常情况处理**

| 异常场景 | 触发条件 | 处理方式 | 用户反馈 |
|----------|----------|----------|----------|
| 网络断开 | 下载中断 | 记录分片状态；票据失效后重新授权 | 网络中断，稍后可以继续下载 |
| 数据为空 | 已发布变体被删除 | 停止下载并上报 | 资源正在维护 |
| 操作超时 | 20 秒无数据进展 | 暂停并重新申请短时票据 | 下载超时，请重试 |
| 权限不足 | 权益不存在或被撤销 | 删除未完成临时文件 | 请先兑换这张壁纸 |
| 校验失败 | 大小、哈希、签名或解密失败 | 删除包并记录安全事件 | 文件校验失败，请重新下载 |
| 空间不足 | 可用空间低于包解压需求 | 不开始安装 | 存储空间不足 |
| 系统能力差异 | 厂商不支持锁屏单独设置 | 使用系统提供的实际选项 | 请在系统页面选择可用范围 |

### 功能 5：管理员访问

**功能描述**

管理员可在管理端登录并进入授权工作区，防止壁纸、兑换码和设备权益被未授权人员操作。

**用户流程**

    打开管理端
      ↓
    输入账号和密码
      ├─ 验证成功 → 建立安全会话并进入工作区
      ├─ 凭据错误 → 记录失败并限制频率
      └─ 账号停用 → 拒绝登录
      ↓
    会话过期 → 返回登录页并保留安全的页面地址

**状态机**

| 状态名称 | 进入条件 | 退出条件 | 用户可见表现 |
|----------|----------|----------|--------------|
| 未登录 | 无有效会话 | 登录成功 | 登录页 |
| 登录中 | 提交凭据 | 成功或失败 | 按钮加载 |
| 已登录 | 会话有效 | 退出、过期或撤销 | 管理导航 |
| 临时锁定 | 连续失败达到限制 | 冷却结束 | 剩余等待提示 |
| 已停用 | 管理员状态停用 | 负责人恢复 | 禁止进入 |
| 会话过期 | 服务端返回会话失效 | 重新登录 | 登录提示 |

**字段规范**

| 字段名 | 类型 | 是否必填 | 长度限制 | 校验规则 | 错误提示 |
|--------|------|----------|----------|----------|----------|
| 管理账号 | 文本 | 是 | 4 至 64 位 | 去除首尾空格，不区分大小写 | 请输入管理账号 |
| 密码 | 文本 | 是 | 12 至 128 位 | 服务端验证，不在前端记录 | 请输入密码 |

**文案规范**

| 场景 | 文案内容 |
|------|----------|
| 空状态 | 请输入管理账号和密码 |
| 加载中 | 正在登录 |
| 操作成功 | 登录成功 |
| 操作失败 | 账号或密码错误 |
| 权限不足 | 当前账号无权访问此页面 |
| 网络异常 | 无法连接服务，请稍后重试 |

**异常情况处理**

| 异常场景 | 触发条件 | 处理方式 | 用户反馈 |
|----------|----------|----------|----------|
| 网络断开 | 登录请求失败 | 不创建本地会话 | 无法连接服务，请稍后重试 |
| 数据为空 | 账号或密码为空 | 前端阻止提交 | 请完整填写登录信息 |
| 操作超时 | 20 秒无响应 | 取消请求 | 登录超时，请重试 |
| 权限不足 | 账号停用或角色不足 | 清除会话并记录审计 | 当前账号无权访问 |
| 频率过高 | 连续失败 | 服务端按账号和 IP 限流 | 尝试次数过多，请稍后再试 |

### 功能 6：分类、壁纸与平台资源管理

**功能描述**

管理员可在分类管理和壁纸管理中维护内容、平台变体和版本，确保用户只看到经过校验并已发布的资源。

**用户流程**

    新建或编辑壁纸
      ↓
    填写基础信息并选择分类
      ↓
    添加平台变体和上传资源
      ├─ Android 4D → 校验 manifest、图层、大小、哈希和预览
      ├─ Android 静态 → 校验图片尺寸和格式
      └─ 未实施平台 → 只保存能力占位，不允许发布
      ↓
    保存草稿
      ↓
    点击发布
      ├─ 校验通过 → 创建不可变资源版本并发布
      └─ 校验失败 → 保留草稿并列出问题

**状态机**

| 状态名称 | 进入条件 | 退出条件 | 用户可见表现 |
|----------|----------|----------|--------------|
| 草稿 | 新建或编辑未发布内容 | 发布或删除 | 仅管理端可见 |
| 校验中 | 上传或点击发布 | 通过或失败 | 校验进度 |
| 校验失败 | 资源或字段不合规 | 修正后重试 | 问题清单 |
| 已发布 | 校验通过并确认发布 | 下架或发布新版本 | 用户端可见 |
| 已下架 | 管理员下架 | 重新发布 | 用户端不再新增兑换 |
| 已归档 | 内容永久停止运营 | 不可直接恢复 | 只读历史 |

**字段规范**

| 字段名 | 类型 | 是否必填 | 长度限制 | 校验规则 | 错误提示 |
|--------|------|----------|----------|----------|----------|
| 分类名称 | 文本 | 是 | 1 至 20 字 | 同状态下名称可重复，标识不可重复 | 请输入分类名称 |
| 分类标识 | 文本 | 是 | 2 至 32 位 | 小写字母、数字和连字符，全局唯一 | 分类标识格式错误 |
| 壁纸名称 | 文本 | 是 | 1 至 40 字 | 去除首尾空格 | 请输入壁纸名称 |
| 壁纸标识 | 文本 | 是 | 2 至 64 位 | 小写字母、数字和连字符，全局唯一 | 壁纸标识格式错误 |
| 分类 | 枚举引用 | 是 | — | 必须引用启用分类 | 请选择有效分类 |
| 封面 | 文件 | 是 | 10 MB | JPEG、PNG、WebP；成功解码 | 请上传有效封面 |
| 平台 | 枚举 | 是 | — | android、ios、harmonyos、universal | 请选择平台 |
| 资源类型 | 枚举 | 是 | — | static、layer_parallax、video、live_photo、theme_package | 请选择资源类型 |
| 资源包 | 文件 | 是 | 200 MB | 类型、版本、签名、解压安全和平台规则通过 | 资源包校验失败 |
| 版本号 | 数字 | 是 | 1 至 2147483647 | 同一平台变体单调递增 | 版本号必须高于已发布版本 |
| 排序值 | 数字 | 是 | 0 至 999999 | 整数，越小越靠前 | 请输入有效排序值 |
| 授权备注 | 文本 | 是 | 1 至 500 字 | 记录原创或授权来源摘要 | 请填写素材授权说明 |

**文案规范**

| 场景 | 文案内容 |
|------|----------|
| 空状态 | 暂无壁纸，创建第一张壁纸 |
| 加载中 | 正在加载内容 |
| 操作成功 | 已保存；发布成功 |
| 操作失败 | 保存失败，请检查填写内容 |
| 权限不足 | 当前账号无权管理壁纸 |
| 网络异常 | 网络异常，草稿已保留在当前页面 |

**异常情况处理**

| 异常场景 | 触发条件 | 处理方式 | 用户反馈 |
|----------|----------|----------|----------|
| 网络断开 | 保存或上传失败 | 保留表单，支持重新上传 | 网络异常，草稿已保留 |
| 数据为空 | 列表无内容 | 展示新建入口 | 暂无壁纸 |
| 操作超时 | 上传 60 秒无进展 | 中止当前分片，可续传 | 上传超时，请重试 |
| 权限不足 | 角色不含内容管理权限 | 服务端拒绝并审计 | 当前账号无权操作 |
| 版本冲突 | 其他管理员已更新 | 拒绝覆盖，重新加载差异 | 内容已被更新，请刷新 |
| 引用中下架 | 已存在设备权益 | 停止新增兑换；保留合法用户下载，紧急下架除外 | 下架后不再对新用户展示 |
| 非法压缩包 | 路径穿越、炸弹包或非法文件 | 隔离并删除临时文件 | 资源包不符合安全规范 |

### 功能 7：兑换码批次管理

**功能描述**

管理员可按批次生成平台通用兑换码，每个码在生成时保存可兑换壁纸数量快照；修改默认额度只影响之后生成的码。

**用户流程**

    点击生成批次
      ↓
    输入批次名称、数量、每码额度和有效期
      ↓
    服务端事务生成随机码
      ├─ 成功 → 保存码摘要和额度快照，生成一次性 CSV
      └─ 失败 → 整批回滚，不产生部分批次
      ↓
    管理员下载并确认保存 CSV
      ├─ 已保存 → 关闭明文交付页面
      └─ 未保存 → 当前短时会话内再次下载
      ↓
    后续修改默认额度 → 仅用于新批次

**状态机**

| 状态名称 | 进入条件 | 退出条件 | 用户可见表现 |
|----------|----------|----------|--------------|
| 生成中 | 提交批次 | 成功或失败 | 禁止重复提交 |
| 待交付 | 批次成功且明文文件仍有效 | 下载确认或超时销毁 | 一次性下载提示 |
| 生效中 | 批次启用且有码有余额 | 全部耗尽、过期或停用 | 可兑换统计 |
| 部分使用 | 已使用部分额度 | 全部耗尽或停用 | 使用进度 |
| 已耗尽 | 所有码额度均耗尽 | 终态 | 只读 |
| 已过期 | 到达有效期 | 终态 | 禁止兑换 |
| 已停用 | 管理员停用 | 管理员恢复且未过期 | 禁止兑换 |
| 生成失败 | 事务失败 | 重新提交 | 无批次记录 |

**字段规范**

| 字段名 | 类型 | 是否必填 | 长度限制 | 校验规则 | 错误提示 |
|--------|------|----------|----------|----------|----------|
| 批次名称 | 文本 | 是 | 1 至 50 字 | 同一自然日可重复，批次编号唯一 | 请输入批次名称 |
| 生成数量 | 数字 | 是 | 1 至 10000 | 整数 | 生成数量应为 1 至 10000 |
| 每码额度 | 数字 | 是 | 1 至 100 | 整数；写入每个码的快照 | 每码额度应为 1 至 100 |
| 有效期 | 日期时间 | 否 | — | 必须晚于当前时间；留空表示长期有效 | 有效期必须晚于当前时间 |
| 兑换码 | 系统生成文本 | 是 | 20 位有效字符 | 加密安全随机；展示时按 5 位分组；数据库只存 keyed hash 和尾号 | 无 |
| 停用原因 | 文本 | 停用时必填 | 5 至 200 字 | 去除首尾空格 | 请填写停用原因 |

**文案规范**

| 场景 | 文案内容 |
|------|----------|
| 空状态 | 暂无兑换码批次 |
| 加载中 | 正在生成兑换码，请勿关闭页面 |
| 操作成功 | 兑换码已生成，请立即保存交付文件 |
| 操作失败 | 生成失败，本次没有创建兑换码 |
| 权限不足 | 当前账号无权管理兑换码 |
| 网络异常 | 网络异常，请先确认批次状态再重试 |

**异常情况处理**

| 异常场景 | 触发条件 | 处理方式 | 用户反馈 |
|----------|----------|----------|----------|
| 网络断开 | 生成结果未返回 | 用幂等编号查询，不重复生成 | 正在确认批次结果 |
| 数据为空 | 无批次 | 展示生成入口 | 暂无兑换码批次 |
| 操作超时 | 60 秒未完成 | 后台继续，前端轮询结果 | 正在后台生成 |
| 权限不足 | 无兑换码权限 | 服务端拒绝并审计 | 当前账号无权操作 |
| 随机码冲突 | 唯一约束冲突 | 服务端内部重试；超过阈值整批回滚 | 生成失败，请重试 |
| 明文文件过期 | 一次性交付文件销毁 | 不从数据库恢复明文，需停用并补发新批次 | 交付文件已失效 |
| 修改默认额度 | 默认值变化 | 不更新历史批次和码 | 新额度仅用于之后生成的兑换码 |

### 功能 8：兑换记录与设备支持

**功能描述**

管理员可在兑换记录页查看额度消耗、设备权益和失败原因，并在同设备自动恢复失败、恢复出厂或更换设备时执行受审计的人工恢复。

**用户流程**

    输入掩码码尾号、设备支持编号或壁纸条件
      ↓
    查询兑换记录
      ├─ 找到 → 打开详情
      └─ 未找到 → 展示空状态
      ↓
    查看码额度、设备和权益
      ├─ 自动恢复问题 → 重新签发当前设备会话
      ├─ 恢复出厂/换机 → 输入新设备支持编号并迁移选定权益
      └─ 可疑请求 → 停用设备或兑换码
      ↓
    服务端事务更新并写入审计
      ├─ 成功 → 原设备对应权益失效，目标设备生效
      └─ 冲突 → 不修改并要求刷新

**状态机**

| 状态名称 | 进入条件 | 退出条件 | 用户可见表现 |
|----------|----------|----------|--------------|
| 正常 | 设备会话和权益有效 | 停用或迁移 | 可重复下载 |
| 待恢复 | 客户端无法匹配原设备 | 自动或人工恢复 | 支持编号可查询 |
| 迁移中 | 管理员提交恢复 | 成功或失败 | 禁止重复操作 |
| 已迁移 | 权益已转移到新设备 | 再次受审计迁移 | 原设备不可下载 |
| 已停用 | 设备存在滥用风险 | 管理员解除 | 拒绝下载 |
| 恢复失败 | 目标设备无效或发生冲突 | 修正后重试 | 原权益保持不变 |

**字段规范**

| 字段名 | 类型 | 是否必填 | 长度限制 | 校验规则 | 错误提示 |
|--------|------|----------|----------|----------|----------|
| 设备支持编号 | 文本 | 查询时任选 | 10 至 16 位 | 精确匹配服务端编号 | 未找到对应设备 |
| 兑换码尾号 | 文本 | 查询时任选 | 4 至 8 位 | 只允许有效字符 | 请输入有效码尾号 |
| 目标设备支持编号 | 文本 | 恢复时必填 | 10 至 16 位 | 必须为已注册且未停用设备 | 目标设备不可用 |
| 恢复范围 | 枚举 | 恢复时必填 | — | 单张壁纸或全部有效权益 | 请选择恢复范围 |
| 恢复原因 | 文本 | 恢复时必填 | 5 至 200 字 | 写入不可变审计记录 | 请填写恢复原因 |

**文案规范**

| 场景 | 文案内容 |
|------|----------|
| 空状态 | 没有符合条件的兑换记录 |
| 加载中 | 正在查询兑换记录 |
| 操作成功 | 设备权益恢复成功 |
| 操作失败 | 恢复失败，原设备权益未改变 |
| 权限不足 | 当前账号无权处理设备权益 |
| 网络异常 | 网络异常，请先确认操作结果 |

**异常情况处理**

| 异常场景 | 触发条件 | 处理方式 | 用户反馈 |
|----------|----------|----------|----------|
| 网络断开 | 恢复请求中断 | 按幂等编号查询最终结果 | 正在确认恢复结果 |
| 数据为空 | 查询无结果 | 保留筛选条件 | 没有符合条件的记录 |
| 操作超时 | 20 秒无响应 | 查询审计和权益状态 | 操作超时，请勿重复提交 |
| 权限不足 | 非负责人执行迁移 | 服务端拒绝并记录 | 当前账号无权处理 |
| 目标设备冲突 | 目标设备已拥有同一权益 | 合并为一份，不重复增加额度 | 目标设备已经拥有该壁纸 |
| 版本冲突 | 记录被其他管理员修改 | 回滚并要求刷新 | 数据已变化，请刷新后重试 |
| 可疑高频迁移 | 同一权益短期多次迁移 | 阻断并要求负责人复核 | 迁移频率异常，已停止操作 |

PRD 落地版已完成，六大盲区均已覆盖。
