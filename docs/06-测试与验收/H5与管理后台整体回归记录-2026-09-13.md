# H5 与管理后台整体回归记录

**执行日期：** 2026-09-13 至 2026-09-14

**工作包：** WP-R01

**结论：** 本地 H5、管理后台与真实 API 的业务回归通过；本轮发现的问题已修复并复测。覆盖用户要求的交互、提示、流程和后台配置前台展示。尺寸工具未生效，390 像素复验不计为本轮通过；原生能力仍按既定边界占位。

**环境：** 本地 API `127.0.0.1:8080`、MySQL 8.4、Redis 7.4、FileStorage；后台 `127.0.0.1:5176`，H5 A `127.0.0.1:5175`，H5 B `localhost:5175`。A、B 为不同站点来源的 H5_TEST 浏览器设备。未操作测试或生产环境。

## 1. 执行方法和素材

真实浏览器操作登录、表单、文件选择、保存发布、分类编辑、精选开关、搜索、兑换、权益、下载及设置演示、客服和教程。浏览器结果同时用正式管理 API、设备 API 和 MySQL 只读断言核对。

再次执行现有 `scripts/verify-local-flow.py` 的 prepare → H5 首兑 → complete，新增真实 4D 夹具，完成额度竞争和整栈重启。静态和 Android 动态壁纸另外从真实后台表单创建、上传并发布；不是 Mock 数据。

本地素材为程序生成的图标 120×120、封面 540×1080、背景及透明前景 600×1200，合法景深 JSON；负例为伪 PNG、非法 JSON。动态正例使用仓库现有教程 MP4。素材只用于联调，不代表正式上架画质验收。

## 2. 真实浏览器回归

| 范围 | 实际结果 |
|---|---|
| 登录与退出 | 空值、错误密码有中文提示；Enter 提交可用，请求中按钮禁用；正确登录、页面切换、退出和再次登录通过。本地登录夹具恢复原密码哈希并删除临时材料。 |
| 空壁纸表单 | 保存草稿时分别提示名称、Slug、一级分类、列表封面、背景层、透明前景层和景深 JSON 缺失；取消关闭，不创建业务记录。 |
| 资源负例与继续保存 | 动态壁纸上传伪 PNG 得到中文格式错误；4D 非法 JSON 得到中文资源校验错误。已创建作品 ID 和最新版本保留，替换合法文件后在同一作品继续保存发布成功。 |
| 静态创建发布 | 壁纸 7 仅选择一级分类、上传封面和原图，保存草稿后发布成功；后台显示 2/2，H5 静态列表与详情展示正确。 |
| 动态创建发布 | 壁纸 8 选择 Android，替换合法 MP4 后发布成功；后台 2/2、平台 Android，H5 中文搜索返回动态卡片和正确封面。 |
| 4D 重新发布 | 壁纸 6 上传新配置并重新发布第二个不可变版本成功；后台 4/4。管理 API 读回新版 PUBLISHED、旧版 RETIRED，三个角色 ordinal 均为 0。 |
| 后台分类映射 | 一级、二级分类改名后，H5 首页、分类标题、子分类按钮同步展示；一级排序改为 2 后首页排在前面。全部分类含 6、7、8，子分类仅含壁纸 6。 |
| 后台精选映射 | 设置精选排序 7，保存后仍保留；取消精选后首页精选移除壁纸 6，普通分类仍可见；重新勾选并恢复排序 7 后保存成功。 |
| 首页与搜索 | 精选、上新、4D、静态切换可用；标题跟随当前模式。中文搜索成功，未命中显示换关键词提示；清空输入后可提交空搜索，首页导航可用。 |
| 兑换提示 | 空码、无效码有可操作中文提示；有效码显示已绑定当前浏览器。实际 CSV 的单额度码由 B 兑换壁纸 7 成功，A 再兑显示“兑换额度已用完，请联系客服”，未增加 A 权益。 |
| 下载与设置交互 | 4D 和静态已获得壁纸均可请求真实下载占位描述，展示进度、演示准备完成、位置选择和完成页。4D 模拟授权失败/重试可用，静态直接完成演示；提示明确浏览器未下载正式资源或修改系统壁纸。重复请求不扣额度。 |
| 下架与重发 | 从后台下架壁纸 7，刷新公开分类后消失；B 的已有权益可打开详情并再次请求下载演示。后台重新发布后公开目录恢复。 |
| 预览试用 | 静态壁纸展示封面和预览演示说明；两分钟倒计时到期退出。再次打开后 Escape 可关闭，退出过渡完成后弹层消失。 |
| 权益与后台一致 | A 的“我的”仅含 6、5、4、2、1，B 仅含 7、1；与管理设备 2、3 的 ACTIVE 权益逐一相同。后台兑换筛选及详情能对应设备、作品、结果和额度变化。 |
| 客服与教程 | 微信复制后读回剪贴板值正确；二维码预览和关闭可用。二维码实际下载文件 9018 字节，与页面 PNG 完全一致。设置教程实际播放，进度前进、播放/暂停状态正确，Escape 关闭；设计教程和设备恢复说明可读，返回“我的”正常。 |
| CSV 一次性交付 | 空批次名称提示正确；生成 1 码、额度 1。交付弹层不被 Escape 意外关闭；放弃销毁警告的“继续下载”返回交付。实际 CSV 79 字节，表头 batchNo/code/totalQuota，1 行合法分组码，额度 1；下载后后台状态 CONFIRMED。明文不写入本记录。 |
| 中文组件与布局 | 分页、对话框关闭、数值控件及设备/权益状态已中文化；1280×720 实际截图核对 H5 首页、兑换弹层和后台编辑器，精选复选框与排序控件不重叠。390 像素工具限制见第 6 节。 |

“清空搜索”先清空编辑框，点击搜索后才提交新条件；输入框内容与已提交结果标题可以暂时不同，属于现有提交方式。

## 3. 真实接口超时与恢复

临时 `docker compose … pause api`，在已登录且已加载的页面触发新请求；等待真实请求超时后检查持续提示，再 unpause 并逐一点击重试。结束时 API readiness 为 UP。

| 页面 | 超时后的结果 | 恢复后的结果 |
|---|---|---|
| 工作台 | 持续“加载失败 / 请求超时”与重试按钮；隐藏统计和表格 | 重新加载统计及表格 |
| 分类、壁纸、兑换码、兑换记录、设备主列表 | 五页均持续提示；隐藏表格和结果总数，不把失败显示成暂无数据 | 五页均恢复真实列表 |
| 批次详情 | 从批次 9 切换至 8，失败时不显示上一批次内容 | 重试显示批次 8 与掩码额度 |
| 兑换详情 | 切换另一记录，失败时清除旧事实和旧设备权益 | 重试显示目标记录 |
| 设备详情 | 从设备 3 切换至 2，失败时清除旧详情 | 重试显示设备 2 的五条权益 |
| H5 搜索 | 持续网络错误和重新加载；不显示“0 张壁纸” | 重试成功后才展示真实未命中状态和 0 张计数 |

本轮没有把暂时停止本地 API 误报为生产故障。请求超时的操作提示要求先刷新确认结果后再重试，避免把未知提交结果当作未执行。

## 4. 本轮发现、修复和复测

| 问题 | 修复与证据 |
|---|---|
| 4D 列表计数漏掉景深配置 | 计数由 3/3 改为真实 4/4，后台已发布作品复验。 |
| 多角色绑定使用全局递增 ordinal | 按每个必填角色写 ordinal=0；真实 4D 第二版发布通过。补充 4D 与 iOS Live Photo 双角色请求测试；iOS 实际配对素材未在本轮发布。 |
| 上传失败后重试重复创建或使用旧锁版本 | 引入 WallpaperSaveError，保留服务端 ID、最新锁版本及已选择文件；真实动态、4D 失败后继续保存成功，单元测试断言仅创建一次。 |
| 编辑普通字段清空精选配置 | HTTP 映射并保留 featuredRank；增加后台精选开关和排序，范围与契约 0～999999 对齐。真实关闭/恢复与前台映射通过，字段保留有测试。 |
| 有子分类时表单强制选择二级 | 按契约改为可选；真实静态、动态仅选一级发布成功。 |
| 浏览器缓存导致保存后仍读旧资源状态 | 管理端与 H5 HTTP 请求使用 no-store；静态资源从旧 1/2 刷新为 2/2，保存/发布/下架后的目录复验通过。 |
| 请求无时限、英文网络/业务错误 | 普通请求 15 秒、上传 60 秒，响应体读取也受时限保护；统一中文错误及字段提示。真实超时和格式负例、网络/下载单元测试通过。 |
| 加载失败 toast 消失后出现空表格、详情串数据 | 六个主页面与三个详情增加持续错误和重试，失败隐藏数据总数，切换详情先清除旧数据；第 3 节逐页真实复验。 |
| 提示夸大 H5 预览、授权、设置及保存能力 | 改为封面预览、模拟授权、设置演示、发起下载并检查记录；真实 4D/静态流程复验。剪贴板失败分支不再误报成功。 |
| 后台控件英文与新控件挤压 | 全局中文 Element Plus，详情状态中文化；精选控件增加间距和换行，截图复验。 |
| 试用按 interval 次数计时 | 改用到期时间计算剩余秒数，避免后台标签页定时器延迟累积；真实两分钟到期、重开与关闭通过。 |

错误码和审计 ID 保留用于诊断，业务提示使用中文；未修改正式 API、Flyway 或冻结 OpenAPI。

## 5. 数据不变量和自动检查

全链路夹具为分类 12 / 13、4D 壁纸 6、批次 8。2026-09-13 23:35:29 complete 成功：A 首兑占 1 额度，5 台新设备争抢剩余 2 额度，恰好 2 成功、3 耗尽。同键同请求重放、新键已拥有不扣额、同键改请求冲突、结果确认、无权益下载拒绝、下线权益保留均通过。

API/MySQL/Redis 整栈重启以及仅清除本夹具设备的 Redis 会话后，公开封面字节和业务事实仍一致；旧会话失效后新挑战和会话可恢复。最终管理 API 与 MySQL 再次读回：

| 事实 | 重启前 | 重启后及本轮最终读回 |
|---|---|---|
| 批次 8 usedQuota / totalQuota | 3 / 3 | 3 / 3 |
| 壁纸 6 ACTIVE 权益 / GRANTED 事件 | 3 / 3 | 3 / 3 |
| 获益设备 | 2、22、23 | 2、22、23 |
| 设备 2 已获得作品 | 6、5、4、2、1 | 6、5、4、2、1 |

2026-09-14 00:05 的最终额外读回：壁纸 6 v12 / 7 v4 / 8 v4 均为 PUBLISHED；6 的资源版本 2 PUBLISHED、版本 1 RETIRED，精选排序 7；7、8 均无二级分类或精选。实际后台生成的批次 9 为 CONFIRMED、1/1，设备 3 权益为 7、1；缺少必填交付票据的下载请求返回 400，不返回 CSV。

| 检查 | 结果 |
|---|---|
| services/api-server ./mvnw verify | 本轮重新执行，18 个快速测试、6 个隔离 Testcontainers MySQL/Redis 集成场景通过。覆盖空库 Flyway、约束、业务状态、文件校验、额度和幂等。 |
| apps/admin-web npm run check | 12 个测试、TypeScript、生产构建通过；包括字段保留、失败继续保存、角色 ordinal、请求超时/网络错误。已有 chunk 体积提示仍存在，不阻断构建。 |
| apps/h5-prototype npm run check / npm run build | 19 个测试、Token 一致性、TypeScript、生产构建通过。 |
| contracts/openapi npm test / npm run baseline:check / npm run bundle | 47 个操作、72 个 Schema、97 个直接错误码覆盖及 9 个冻结文件 SHA-256 检查通过，bundle 成功。 |
| 提交检查 | git diff --check、暂存区 gitleaks、文档链接及敏感/运行时文件排除检查通过。 |

本地 Docker 守护进程最初未运行，恢复 Colima 与本地栈后才执行回归。最终读回辅助脚本曾因误用 DTO 字段及遗漏票据预期写错而失败；按真实嵌套分类、可省略 null 字段、quotaPerCodeSnapshot 和缺票据 400 修正后读回通过，不计这些脚本失败为产品成功证据。

## 6. 清理与未测边界

本地 API 已解除暂停、readiness 为 UP。登录夹具原哈希恢复，临时凭据、原哈希备份和完整兑换码夹具删除，脚本会话撤销，浏览器管理员退出；测试页面关闭，临时尺寸覆盖重置。脱敏报告仅留在忽略的 `.runtime`；本地测试业务数据和浏览器实际下载文件保留。Git 不含运行时素材、数据库、明文码、登录信息、下载 CSV、构建产物或用户的 `ui资源`。

本轮通过视口能力请求 390×844，但现有页和新页读回均仍为 1280×720；因此只计实际默认视口截图及 H5 居中移动布局核对，不能宣称本轮完成 390 像素复验。WP-P07/P09/P10 的既有 390 像素证据仍保留，当前工具限制不覆盖新复验。

未执行浏览器永久删除/归档的最终动作，CSV 放弃销毁只验证警告和取消返回；相关 API 状态约束由集成测试覆盖。未人为禁用剪贴板权限，因此失败文案分支只有代码检查，实际复制成功已验证。二维码/CSV 的浏览器下载事件工具未返回事件，但系统下载目录中的实际文件和内容均验证通过。

真实 4D 姿态/动态壁纸播放、iOS Live Photo 真素材配对与设置、HarmonyOS 包、正式 App Provider、加密资源交付、系统权限与设置、功耗/内存/厂商设备矩阵、测试和生产发布未执行。H5 演示完成不代表这些能力通过，Flutter 正式工程不在本轮范围内。
