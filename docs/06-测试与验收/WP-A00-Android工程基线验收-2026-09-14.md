# WP-A00 Android 工程基线验收

日期 2026-09-14；基线 d3b8536；分支 codex/wp-a00-android-internal。工作状态见 PM-002。

已实现正式 Flutter/Kotlin 工程、现有 Token 导出、首页/我的切换、纯 Dart 六类平台边界、未实现显式 unsupported、local/prod 标识与网络隔离、测试签名边界、工具链清单和 Android CI。

## 当前证据

- Flutter 3.38.10 / Dart 3.10.9 / revision c6f67dede3；本机 SDK 非标准 channel，doctor 有提示，未自行升级覆盖已有工具链。
- Temurin 17.0.19+10，SDK 36，NDK 28.2.13676358，Gradle 8.14，AGP 8.11.1，Kotlin 2.2.20；应用最低 API 26，内部版本 1.0.0+10001。
- flutter analyze 通过；flutter test 3 项通过：首页/我的切换、正式/非调试 HTTP 拒绝、所有平台效果/位置及占位操作不误报成功。
- OpenAPI lint 与 47 操作/72 Schema/97 错误码检查通过，历史 9 文件 SHA-256 校验通过。数据库、Java DTO、API 和历史冻结字段均无变化，不涉及迁移。
- Colima 恢复后原有本地 API/MySQL/Redis healthy，readiness 返回 UP；没有复制或重置业务数据。
- 初次 Dart 分析发现生成 Token 的跨 lib 导出不可见，修正为包内符号链接引用原生成物，分析/测试复测通过。

## 尚缺证据

Android debug/release 构建与测试签名验证已通过；远程 CI 尚未运行。ADB 列表为空，已请求用户连接解锁手机并授权 USB 调试，未在真机安装或启动。不得标记 A00 已完成。鸿蒙/iOS 未构建，身份、兑换、下载、效果和性能属于后续任务，未计通过。

配置参考：[Flutter flavors](https://docs.flutter.dev/deployment/flavors)、[Android network security](https://developer.android.com/privacy-and-security/security-config)。真机系统设置行为仍需单独实测。

## 构建补充

- 官方 Flutter 3.38.10 tag 经 git ls-remote 读回为 c6f67dede3d4aa1aa7a69dd56a3494a5cde6cc80，与本机一致。
- 首次 debug 子进程启动失败；固定 Gradle UTF-8 后重试成功（185.5 秒）。合并 Manifest 确认 local 标识、10001/1.0.0、minSdk 26/targetSdk 36。
- debug APK SHA-256：84ee8e52d2751451744a0312f39cc3635084f62d05986e598e651f3ac328bf7d。apksigner verify 通过，签名为 Android Debug，仅内部测试。APK 位于 apps/mobile/build/app/outputs/flutter-apk/app-local-debug.apk，不进入 Git。

- localRelease 构建通过（211.0 秒、41.8 MB），APK SHA-256：1d287c385d6ead7224d32dd8cf607a8b505f6c2e80edbc6de34e1ac38a504e1a；apksigner verify 通过，Android Debug 测试签名，与 debug 一致。
- Android 35 x86_64 只读模拟器安装成功；首次 am start 超时、UI dump 无节点，截图显示应用首页后覆盖 System UI 无响应弹窗。关闭弹窗后复测读到首页标题、文案及双 Tab 节点；资源压力导致首次失败，不计性能通过。
- 模拟器点击我的 Tab 后，UI dump 验证“我的壁纸”与正式身份待接入提示；双 Tab 补充启动验收通过，真机仍未验收。原始节点/截图仅保存在忽略目录 .runtime/android-evidence。
- prodRelease Manifest 任务通过；脚本核验 localDebug/localRelease/prodRelease 的应用标识、备份关闭、明文限制和 release 不可调试均通过。未配置生产签名，不是生产安装验收。
