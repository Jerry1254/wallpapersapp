# WP-A01 Android 真实目录验收

日期 2026-09-14。A00 提交 6f4ffd3 已推送功能分支，缺少真机验收；按用户授权继续独立目录实现，未绕过主线门禁。

## 实现

首页沿用现有 Token 与 H5 内容层次：品牌/搜索、分类、精选/上新/4D/静态筛选、两列作品和首页/我的 Tab。分类页含子分类，搜索保留中文；真实 HttpClient Repository 使用公开 API 和 ANDROID 过滤，ID 保持 String。网络超时 15 秒，响应限制 4 MiB，失败可重试，封面错误降级；翻页失败与同查询刷新失败保留已有列表，旧请求晚返回不覆盖新查询。详情按钮明确接入中，本包不宣称详情/身份/权益/设置完成。

## 自动证据

- dart format、flutter analyze 通过。
- Flutter 8 项测试通过（新增刷新失败重试保持第一页用例通过）：基础 Tab/网络边界/不支持能力，加上查询竞态、分页失败/刷新失败保留列表与去重、销毁后晚响应、未知类型和媒体 URL 边界。
- flutter test test/catalog_local_test.dart --dart-define=LOCAL_API=http://127.0.0.1:8080/api/v1 通过：读取现有分类、首/次页、标题中文搜索、详情和不存在作品 404；仅只读，没有新增/重置业务数据。普通 CI 跳过依赖本机数据的该测试，不能冒充执行。
- localDebug APK 构建通过（30 秒）；产物在 apps/mobile/build/app/outputs/flutter-apk，Git 忽略。
- 评审发现刷新失败且原列表仍可翻页时，错误按钮可能错误请求下一页，修正为记录失败操作并重试对应请求。

## 开工阶段待验收（历史）

模拟器首页/分类与断开转发后重试恢复通过；真机未连接，后台内容真机可查、真机断网恢复和视觉人工验收尚未完成。不计 A01 已完成。无 API/DTO/数据库改动，不涉及迁移。未测其他平台和系统壁纸效果。

## 模拟器补充

Android 35 x86_64：升级安装成功，首次 am start 等待超时，随后截图与 UI dump 确认真实首页分类和作品渲染。点击回归分类后显示静态/动态/4D 三项；移除 adb reverse 后下拉刷新，出现网络错误且三项保留；恢复 reverse 点击重试后错误消失、三项仍在。未执行模拟器中文键盘输入，中文搜索由真实 API 用例验证；真机输入体验和性能不计通过。

GitHub Actions [34795251558](https://github.com/Jerry1254/wallpapersapp/actions/runs/34795251558) 在 59fe8b4 全部门禁通过。

## 红米补齐与工作包完成

10013 ARM64 internal release 已在红米 Android 13 真正新装，真实 HTTPS 首页读出后台发布的验收分类、静态/视频/4D 三类作品与封面；正常分类入口显示全部三张，正常详情跳转通过。移除该设备 8443 reverse 后下拉刷新，分类与作品列表各显示错误/重试，原分类与作品保留；恢复 reverse 后分别正常重试，错误消失、内容恢复。

在本轮隔离 API 8083 显式运行现有 catalog_local_test（LOCAL_API=http://127.0.0.1:8083/api/v1），1 项综合测试通过、0 跳过，实际包含分类、中文标题搜索、pageSize=1 的首/次页、分类筛选、详情及缺失 ID 的 404；该只读测试是 App Repository 的当前 API 证据，不冒充手机中文键盘输入测试。默认 Flutter 38 项通过/1 项本地用例按配置跳过的记录保持原样，另列这次显式执行结果。

代理已检查实际新装首页/分类截图：背景、暖色选中态、字体、间距、两列卡片及首页/我的导航沿用 H5 已确认 Token/层次，文字和控件无覆盖；测试分类名称较长时正常换行。此为代理视觉核对，未声称用户另行确认新版视觉，也不作为其他机型验收。A01 实现/相关自动与真实 API/红米显示及断网恢复门禁补齐，状态已完成；详细流程见 [A10](WP-A10-Android全链路真机验收-2026-09-14.md)。
