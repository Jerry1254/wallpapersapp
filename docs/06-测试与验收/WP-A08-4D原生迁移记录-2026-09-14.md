# WP-A08：4D 原生迁移记录

状态：待验收。已完成实现、自动检查、红米基础功能、物理倾斜及整机重启恢复；A10 固定条件长时性能仍待验收。目标设备为已连接的 Redmi M2012K11AC（Android 13、1080×2400）。

## 迁移范围与来源

迁移来源为仓库 PoC `app/src/main/java/com/livewallpaper/parallaxdemo` 的 `TiltSensor`、`ParallaxScene`、`ParallaxPreviewView` 和 `ParallaxWallpaperService`。保留旋转向量优先级、首帧姿态校准、角度归一化、分层深度和居中覆盖逻辑。正式实现改用签名校验后的私有安装包，读取配置中的角度、平滑、强度、缩放、透明度和混合模式；使用独立渲染线程、可见性门控、解码预算和资源引用保护。

## 验收与性能方案（先固定条件）

- 功能：App 独立预览、真机实际倾斜、旋转方向、最大位移不露黑边、无传感器静态降级、系统实际选择目标、取消保留原资源、缓存保护、隐藏/关闭屏幕释放、重新可见及进程重建恢复。
- 单元验证：角度跨 ±π、四种屏幕方向、位移边界、不同平滑系数和时间间隔、配置非法值及总解码预算；同时回归 A07 设置结果与安全包校验。
- A10 性能复测素材：1080×2400，两层（背景与透明前景），正常混合，30 fps 上限；可见预热 5 分钟后 30 分钟，隐藏 10 分钟。可见首帧 ≤3 秒、隐藏后停止绘制/注销传感器/释放图片 ≤1 秒；预热后 PSS 增长 ≤20 MiB，无 ANR/崩溃。记录实际帧率、CPU、电量和温度及静态基线差异，不预先宣称功耗合格。
- 自动验证只证明渲染、生命周期和资源保护；真实倾斜的视觉方向需物理操作确认。小尺寸夹具不计为上述长时性能结果。

## 当前结果

| 项目 | 实际结果 |
|---|---|
| 能力查询 | 静态/视频/4D 均可预览，4D 系统设置入口可用，姿态传感器存在；没有独立动态锁屏能力承诺 |
| 签名夹具安装 | 三类包安装通过，4D 使用网格背景与透明黄色圆形/十字前景；当前 4D 版本 3、资源版本 ID 2304，清单 SHA-256 为 ddee5c455c2b6f34d3e4f2cf6468f250595425a29a9b500571956716a6ac94e4 |
| App 4D 预览 | 实际渲染两层画面并注册传感器；结束后 completed 仅表示预览完成，不表示系统设置成功 |
| 无传感器降级 | localDebug 专用参数模拟不可用，显示完整静态画面与降级提示，结束 completed；未拿有传感器的红米冒充无传感器真机 |
| 系统取消 | 系统预览实际渲染；返回 cancelled，系统 ID 保持 32/-1，原视频服务保留 |
| 系统确认 | 当前共享壁纸状态下点击“设置壁纸”直接确认，系统 ID 变为 33/-1；ADB 独立查询桌面组件为 ParallaxWallpaperService。App 无法读取组件，返回 unknown 并保留已渲染资源；没有伪报 completed 或独立锁屏成功 |
| 桌面绘制 | 正式非预览 Engine 实际渲染，sensorRegistered=true，decodedBytes=2097152；小尺寸夹具不计为性能达标 |
| 隐藏暂停 | App 前台后 Engine rendering=false、sensorRegistered=false、decodedBytes=0、installedId=null；返回桌面复验同一资源并重建图片 |
| 缓存保护 | 清理 791070 字节未使用资源后，当前 4D 仍存在并可恢复；新下载但未使用的静态/视频被清理，之后重新下载可预览 |
| A07 视频回归 | 4D 已设置时视频独立预览及结束通过，未替换 4D 系统资源；旧视频提交指针在首次 4D 提交时迁移到独立保留位置 |
| 息屏/唤醒 | KEYCODE_SLEEP 后 Dozing 并释放传感器/图片；KEYCODE_WAKEUP 后 Awake、锁定界面仍 showing=true，4D 同一资源恢复渲染。未读取锁屏个人通知或操作解锁凭据 |
| 更新安装/进程重建 | adb install -r 覆盖为正常 main 调试 APK，PID 15287→18544（首次正常 main 更新；最终绘制保护补丁 PID 20202，再次覆盖安装亦恢复）；系统服务从私有保留指针重新校验，加载同一 4D 资源，传感器与绘制恢复 |
| 崩溃/ANR | 本次包日志没有 AndroidRuntime FATAL；系统 lastanr 返回 no ANR has occurred since boot。仅为本次基础验证结果 |
| 实际倾斜与横转 | A09 正式 API 作品 3/版本 3 的 4D 预览，用户实际倾斜后回复“有动”；系统强制 90°/270°再恢复 free 均绘制正常，实际媒体区域黑色像素比例 0。物理角度精度未测，数学方向与裁切边界另有自动验证 |

系统选择页随原有桌面/锁屏状态变化，本轮仅看到直接确认按钮；不把 A07 曾出现的桌面/两者选项套用为 A08 本轮选择页证据。独立锁屏、整机重启、1080×2400 长时性能及新版正式 API 从发布到权益下载的联调继续在 A10 补齐。

## 自动检查与构建

- Flutter analyze：无问题；Flutter test：28 通过、1 个既有本地集成条件跳过。
- 原生 JUnit：24 通过（安全包 13、设置结果 3、4D 几何 6、配置 2），失败/错误均为 0；lintDebug 通过（16 项 Warning，没有 Error）。最终原生/lint 检查构建成功，耗时 1 分 29 秒；prod Manifest 检查构建通过。
- localDebug/localRelease/prodRelease 合并 Manifest 均验证两个壁纸服务由 BIND_WALLPAPER 保护、查看器不导出；无 QUERY_ALL_PACKAGES/广泛外部存储权限，网络/备份/调试策略通过。
- 正常 main arm64 localDebug 构建成功；`.runtime/android-evidence/a08-redmi-main-final-debug.apk` SHA-256：a0403123883576d0580db1b6378d3b470e9a8327387c78bdcc2d0a48e470f912，apksigner 校验通过，已在红米覆盖安装。
- 正常 main arm64 localRelease 构建成功（16.9 MB，10.3 秒），apksigner 校验通过；`.runtime/android-evidence/a08-local-final-release.apk` SHA-256：079fbc4a2ded48f12cf917661fc26d02d57ec5493054de21df15104433c5ab58。local 包使用内部测试签名，默认 api.invalid，不能当作已部署的在线候选版；A11 另交付正式内部候选清单。

原始构建、界面和生命周期证据在忽略的 `.runtime/android-evidence/a08-*`，签名夹具及私钥在忽略的 `.runtime/android-native-fixture`。这些运行时文件不提交 Git。

## 失败、修复与迁移差异

首次重跑夹具时，相同版本的清单因 Java Map 字段迭代顺序变化产生不同摘要，客户端正确拒绝覆盖。正式发布本身按资源版本保存不可变包并幂等复用，无需因此放宽安全校验。夹具现按来源摘要复用原包，来源变化才递增版本；连续准备三类包的加密内容、描述和内容密钥不变检查已通过。

与 PoC 相比，正式版本采用每个查看器/Engine 独立线程与租约，异步复验私有资源后才解码；可见性代次阻断旧加载和旧帧。角度、平滑、强度、层顺序、透明度和 normal/screen/add 来自严格配置；位移限制在实际覆盖余量内，方向随屏幕旋转重新校准；按时间间隔调整平滑，最多 30 fps，无传感器时仅绘制静态帧。两类动态服务各自保留资源并兼容旧视频指针，取消/失败不覆盖提交版本。

此前联调准备：已创建独立本地 Android API 环境（8083，MySQL 3311、Redis 6391，独立数据/凭据/存储），当时 1.2.0 API 的 V1/V2 空库迁移及完整 db/redis 健康检查通过；手机通过 ADB reverse 访问 8083 的公开分类 HTTP 200。当时目录为空，不将连通性当作完整业务下载验收。

A09 已将同一隔离 API 升级为 1.3.0/V3，并通过管理 API 发布三类生成素材；红米完成真实兑换、正式签名下载和安全安装。4D 物理反馈与屏幕旋转结果见 [A09 记录](WP-A09-两分钟试用实施记录-2026-09-14.md)。显式 force-stop 后 MIUI 回到默认系统壁纸，不能据此声称普通进程死亡恢复失败；随后通过正常系统设置页选择“主屏幕”恢复正式 4D，ADB 确认 ParallaxWallpaperService。

A10 实际整机重启 BOOT_COUNT 349→350，无线 ADB 重连后确认系统组件仍为 ParallaxWallpaperService、正式引用清单与重启前相同；本应用 QJPlayback 重新记录 rendering=true、sensorRegistered=true。“我的”重新读出原三张权益；[A10 记录](WP-A10-Android全链路真机验收-2026-09-14.md) 保存实际条件。当前小规格素材恢复通过，仍不替代固定规格长时性能。
