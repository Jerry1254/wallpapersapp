# WP-A10 Android 全链路真机验收

**最新状态：已完成，按用户于 2026-09-14 定义的 Android 1.0 范围。** 10015 已安装到红米，最后实际体验由用户验收；华为/荣耀 2.0、苹果 3.0 未开始。本文旧进行中/待验收及试用/固定测量门禁属于历史记录，最新结果优先。


状态：进行中，按用户最新 Android 1.0 两类交互范围收口。下面原性能/压力方案为历史，未完成报告不记通过且不补测；最终实际体验由用户验收。

用户于 2026-09-14 明确取消内存不足、存储空间不足的专项压力测试。本轮不再执行这两项，也不作为 A10/A11 阻塞门禁；该范围调整不表示实测通过。已准备的超大 4D 版本保持未发布，专用模拟器已停止，保留既有保护代码；正常缓存清理、播放、重启、升级及业务闭环继续验收。

## 环境与机型

只使用本工作树的隔离本地 API 8083、MySQL 3311、Redis 6391 和生成素材；不部署生产，不同步其他环境业务数据。手机通过已授权无线 ADB reverse 访问电脑 API。目标设备为 Redmi M2012K11AC / alioth，Android 13，1080×2400、arm64；其他机型尚未验收。

现有 `.local` App 保留数据和 Keystore。新增独立 `.internal` 候选内测安装标识及独立测试签名，用于新装、升级、卸载后的身份恢复边界测试；生产包仍独立且没有生产签名。候选内测只连接 HTTPS loopback API，通过 8443→8083 本机代理与 ADB reverse 访问，不要求手机配置数据库或手动部署 API。

仅 internal flavor 信任构建时配置的公共 loopback TLS 证书：Android 原生连接采用仅覆盖 localhost/127.0.0.1 的 network security config；Dart 添加同一公共证书，保留证书链、有效期与主机名校验，没有 accept-all 回调。做法依据 [Android 官方网络安全配置](https://developer.android.com/privacy-and-security/security-config) 和 [Dart SecurityContext 文档](https://api.dart.dev/dart-io/SecurityContext/setTrustedCertificatesBytes.html)。证书私钥与签名 Keystore 只存忽略的运行时目录；生产 flavor 不添加内测信任。

## 已执行

| 检查 | 实际结果 |
|---|---|
| 前序试用及正式交付 | 红米三类实际试用、视频/静态 120 秒到期、提前退出、后台/进程沿用原截止、三类正式兑换/签名下载/安全安装/预览通过，见 [A09](WP-A09-两分钟试用实施记录-2026-09-14.md) |
| 物理 4D 效果 | 正式预览用户倾斜后回复“有动”；90°/270°及恢复竖屏画面覆盖通过，物理角度精度未测 |
| 整机重启 | BOOT_COUNT 349→350；重新配对/连接无线调试并恢复 reverse，showing=false。系统仍绑定 ParallaxWallpaperService，正式安装引用清单 SHA-256 与重启前相同；本应用 QJPlayback 日志重新出现 rendering=true、sensorRegistered=true |
| 身份/权益恢复 | 重启后的 normal main，“我的”重新读取三张权益；管理 API 仍为 Android 设备 1、权益 3，同一安装继续真实签名请求；试用状态/目录为空。该重启前没有活跃试用，不能以此证明活跃试用跨重启放弃 |
| 隔离 API 重启 | 只重启 8083 的不可变 1.3.0 Jar，为此隔离实例增加 `.internal` 正式 Android scope；设备/权益保留，HTTPS 公开分类 HTTP 200 |
| HTTPS 真实验证 | 自动测试使用实际 TLS 服务验证：非 internal 环境不信任测试证书、internal 公共证书握手成功、错误主机名拒绝；缺少证书、非 loopback 和明文 internal 配置拒绝 |
| Flutter 当前回归 | 在 apps/mobile 运行 analyze 无问题，38 项测试通过，1 项既有显式本地集成默认跳过。仓库根目录的 analyze 因独立包无根 package_config 无效，不作为 App 回归结果 |
| 固定条件素材准备 | 通过管理 API 制作视频不可变版本 2/资源 4（1080×2400、30 fps、10 秒 H.264）、4D 版本 2/资源 5（同分辨率背景+透明前景）及静态版本 2/资源 6；正式/受限包构建通过，三者版本 2 均已发布；旧版本字节不覆盖 |
| 内测 release 构建/新装 | ARM64 internal release 0.1.0+10010 构建、apksigner 校验与红米 install -r Success；新装此前不存在的 `.internal`，没有清除 `.local`。APK SHA-256：`1f5e144e352a36d4c2924efdf532d2911ae29fa5324a2655d85e05c31c80e938`；独立测试证书 SHA-256：`f46b13574dc73b8bba32d008d4967306edf6d06181b174569ba94b0b7709bfa8`。合并 Manifest debuggable=false，HTTPS 真实目录及封面加载通过 |
| 当前原生/环境回归 | 原生 JUnit 30 项，失败/错误/跳过 0；lint 0 Error/25 Warning；localDebug、localRelease、prodRelease、internalRelease 四变体身份/明文/备份/查看器/壁纸服务门禁通过，prod 无内测 TLS/诊断配置 |
| 新装正式身份 | `.internal` 经真实 Keystore/Android Provider 注册为独立设备 2、权益 0；原 `.local` 设备 1 仍为权益 3，不跨安装自动复制权益 |
| 内测正式交付/系统设置 | 为新包通过正常 MIUI 界面允许动态壁纸服务，重新检测后桌面可用；兑换 4D 成功扣 1，真实 HTTPS 正式票据/下载/Keystore 解密/验签/安装/原生预览通过。系统选择“设置壁纸→主屏幕”，ADB 确认 `.internal` ParallaxWallpaperService |
| 4D 更新断网 | 管理 API 发布不可变版本 2/资源 5 后移除 8443 reverse；正常更新提示无法连接，旧版保留，断网状态下旧版全屏原生预览仍可用 |
| 损坏包/传输中断 | 独立本机故障代理只修改受控正式下载的加密字节，分别翻转首字节及只发送首块的一半；两次实际 HTTP 200 下载分别提示校验失败/下载中断，旧版仍安全安装，损坏后正常打开旧版预览通过 |
| 正在下载时取消 | 故障代理首次字节延迟 16 秒，界面实际 0%/正在下载；正常点击“取消下载”，显示下载已取消、已安装资源/预览/设置入口保留。此前 8 秒延迟请求未及时点击取消而正常完成，不将其记为取消测试 |
| 正常重试/更新 | 恢复正常下载，实际正式版本 2 安装/原生预览/系统确认通过；非预览 Engine decodedBytes=20736000，对应两张 1080×2400 ARGB 图像，resourceLoaded/rendering/sensorRegistered 均 true |
| 首轮全规格 4D 测量 | 0.1.0+10010 的首个可见观测 676ms，首样 PSS=133917KiB；全规格实际约 5.13～5.19fps，在预热中发现软件 Canvas 路径性能不足而停止。测量未完成，不记长时通过；脚本已恢复原屏幕超时 300000ms。实际 BOOT_COUNT=350、亮度 14/手动、AC 充电、电量 100%、34.7～35.0℃ |
| 独立签名身份并发额度 | HTTPS 实际注册 5 个独立 ANDROID/PLATFORM_PUBLIC_KEY 签名身份并发争抢本地 1 码/2 额度：2 个 GRANTED/1、3 个 CODE_EXHAUSTED/0，后台 usedQuota=2/2；成功者同 key 重放/原结果查询一致，新 key 已拥有不再扣。该项为真实后端签名并发，5 个签名密钥由主机生成，不宣称 5 台物理真机 |
| Android 云端回归 | 提交 83b3cae 的 [Android Actions 34837506445](https://github.com/Jerry1254/wallpapersapp/actions/runs/34837506445) 成功：Flutter analyze/test、原生 JUnit/lint、local debug/release 构建及四变体 Manifest 门禁 |
| 下线权益与授权 | 独立主机 Android 签名身份通过 HTTPS 领取并校验正式静态版本 2 字节；管理 API 下架后 ACTIVE 权益摘要仍在，旧票据读取 401、新正式票据 422；finally 通过正常管理 API 重新发布同一不可变版本。该项为真实后端签名/状态验收，不作为手机画面验收 |
| 二进制下载错误修复 | 首轮下架旧票据实际返回 500：Spring 错误响应在 Accept=octet-stream 时媒体协商失败。统一 ErrorEnvelope 显式 JSON 后实际复测返回 401；正式/试用失效、缺少票据、429/Retry-After 及正常二进制流由新增 HTTP 边界测试覆盖，集成的两类读取也使用与原生相同的 binary Accept |
| 会话失效/原结果恢复 | 只强制过期隔离 Redis 中该主机签名身份的会话键，旧请求实际 401/SESSION_EXPIRED；同一 RSA 凭据重新挑战后权益响应逐字段一致，原并发兑换 key 仍返回原 GRANTED/1，后台 usedQuota 仍 2。显式挑战为主机后端证据，不冒充手机自动续期，也不宣称等待完整 TTL |
| 契约说明修正 | 下架响应的遗留“默认仍可交付”与 A05 已冻结的状态授权不一致，明确权益保留、停止新正式交付/旧票据新读取、已安装资源可继续使用。OpenAPI 1.3.1 只修订版本/该说明及 API-001；原 1.3.0 快照逐字节归档，DM/DB 和 V1/V2/V3 及其他冻结字节保持不变 |
| API/既有网页回归 | 新增二进制错误修复后 API 35 单元+9 隔离 MySQL/Redis 集成测试通过（0 失败/错误/跳过），H5 19 测试/Token 检查/类型检查/构建、后台 13 测试/类型检查/构建通过；新 OpenAPI 51 操作/77 Schema/119 直接错误码覆盖及 17 冻结摘要通过 |
| 内测 10013 覆盖升级 | install -r Success，正常 main→我的真实读取原 4D 权益；后台原设备 2、同一安装凭据编号与权益 ID 保留，原 `.local` 设备 1 仍为三张权益 |
| 手机自动会话恢复 | 强制过期隔离 Redis 中设备 2 的唯一会话后，正常“刷新我的权益”恢复同一 4D 权益；后台仍同一凭据/权益 ID。该项是真机缓存 Token 失效后的客户端自动续期，未等待完整 TTL |
| 手机未知兑换/进程恢复 | 对视频首次兑换，代理完整读取后端 201/GRANTED/1 后丢弃响应，手机显示连接失败并要求确认原结果；只 force-stop `.internal` 再启动，正常“确认上次兑换结果”显示兑换成功，真实权益列表出现视频/4D，再确认显示没有待确认兑换。后台同原 key 只有一条最终 GRANTED/1，usedQuota 在提交后/恢复后均为 2/3，没有重复扣减 |
| 当前 APK 云端回归 | 提交 0353812 的 [Android Actions 34841301254](https://github.com/Jerry1254/wallpapersapp/actions/runs/34841301254) 成功，和上述本地原生/lint/release/四变体检查共同验证 10013 输入 |
| 内测正式视频交付 | 视频版本 2/资源 4 经真实票据、HTTPS 下载及 Keystore 解密/验签/安装成功；正式原生全屏预览两次画面不同，正常结束。系统预览→设置壁纸实际提供“主屏幕”“主屏幕和锁定屏幕”，选择后 ADB 确认 VideoWallpaperService；App 保持位置不可读取的提示，不冒充独立锁屏成功 |
| 内测正式静态交付 | 正常兑换第三张静态权益、HTTPS 正式版本 2/资源 6 安全安装、无倒计时原生预览与结束通过；桌面实际切换为 MIUI ImageWallpaper，锁屏/两者的正常按钮分别显示已确认系统设置及共用新画面。桌面切换时系统 wm_relaunch_resume_activity 重建 Main，回到首页；没有本包 crash buffer FATAL，重新读取三张权益并查看同一已安装静态资源通过 |

内测构建先遇到 Gradle 生成证书资源未正确声明所有消费者依赖，已改用 AGP 的 `addGeneratedSourceDirectory`；之后 lint 要求 domain 显式 includeSubdomains=false，修正后完整 release 构建成功。首次新装签名请求被代理拒绝分块请求体，已增加有大小/格式限制的 chunked 解码；实际分块 HTTPS 分类请求 HTTP 200，新装 Android 注册通过。没有通过关闭 lint、放宽 TLS 或启用 H5_TEST 绕过失败。

上述内测 APK 包内公共证书字节与配置一致，未含 TLS 私钥/Keystore。证书有效至 2026-12-13，仅限当前本地内测环境；当前包是 A10 测试输入，A11 仍需固定提交、最终构建与升级验收，不把这次新装当作整个候选版已完成。

全规格帧率暴露 A08 软件 `lockCanvas` 路径瓶颈，已改为 API 26 起提供的 `lockHardwareCanvas`，保持相同分层配置、几何、混合和可见性门控。依据 [SurfaceHolder 官方接口](https://developer.android.com/reference/android/view/SurfaceHolder#lockHardwareCanvas())；Android 壁纸 SurfaceHolder 也有相应实现。

硬件 Canvas internal release 0.1.0+10011 构建/签名校验及 install -r Success，APK SHA-256 为 `b1148eff4353c5aff6fe062b7bd30ff8b2fad1f393d821c07be4786638090c4d`。覆盖升级后系统 4D 仍加载正式版本 2（decodedBytes=20736000），实际首帧观测 233ms，预热阶段帧率约 28.44～28.46fps；原生测试/lint 再次通过。以上是实际改善和升级恢复证据，完整 5+30+10 分钟测量仍在进行，不记长时通过。

上述 10011 已完成预热 5 分钟及实际可见 1800.214 秒：可见帧率中位数 28.548fps、样本范围 25.994～28.660fps；预热后 PSS=101755KiB，最大增长 1978KiB，满足 ≤20MiB 阈值。首轮隐藏首次观测已释放用时 629ms，但连续隐藏约 6 分钟后手机焦点变为 MIUI 启动器覆盖窗口，服务 visible/rendering/sensorRegistered 重新为 true；因此该隐藏段中断，完整报告 completed=false，不记隐藏失败或长时全程通过。中断原因尚未确定，记录状态事实，不归因用户或应用崩溃。

补测首次读取发生在可见性回调前，暴露测量脚本把“已发起切换”时间误记为“已释放”时间；改为确认全部播放/绘制/传感器/图片状态释放后计时，并校验应用仍在前台。保留这次未完成观测，不修改原报告。随后从已确认可见状态重新进入前台，实际释放观测 430ms，固定 10 分钟隐藏补测正在执行；只补测隐藏，不重复计算可见段或虚构连续 45 分钟。

隐藏补测现已完成实际 600.091 秒，全程 visible/rendering/sensorRegistered/resourceLoaded 为 false、decodedBytes=0，满足隐藏释放 ≤1 秒及保持释放的条件；未发现当前应用 PID 的 crash buffer FATAL 或 lastanr 中的本包记录。屏幕超时已恢复 300000ms。可见段与独立隐藏补测分别成立，不宣称一条无中断的连续 45 分钟报告。充电/亮度固定条件下，4D 可见/隐藏的 CPU、PSS、温度原始样本保留；静态基线及视频实测仍待。


视频固定条件测量将使用自有 MediaPlayer 的 API 26 frames/droppedFrames/decodeErrors 指标，依据 [Android 官方 MetricsConstants](https://developer.android.com/reference/android/media/MediaPlayer.MetricsConstants)。只有 debug/独立 internal 诊断启用时采集，安全 dump 仅增加这三个整数，指标缺失不会转成零；不改变播放或生产诊断门禁。新增采集代码的原生 30 项测试及 lint 0 Error/25 Warning 通过，脚本语法检查通过。提交 aab834b 的 ARM64 internal release 实际版本 1.0.0+10012 已构建并通过签名/四变体 Manifest 检查，APK SHA-256 为 `06f02d07024b2c21c9b53890f93d05fc296c3a96764a8da90484a2858f2aebd2`；尚未安装及采集视频真机指标。该构建使用 pubspec 默认版本名，不将其记为 0.1.0 或公开发版。当前正在测量的 4D APK 仍为上述 10011，没有中途安装或更换测量输入。[Android Actions 34839214752](https://github.com/Jerry1254/wallpapersapp/actions/runs/34839214752) 与 API 修复的 [API Actions 34839176057](https://github.com/Jerry1254/wallpapersapp/actions/runs/34839176057) 均成功。

视频 isPlaying 只表示播放器启动，不能当作首帧；诊断增加由 MEDIA_INFO_VIDEO_RENDERING_START 回调设置、释放时清除的 rendering 状态。测量脚本对视频和 4D 均等待实际 rendering 后计时；视频播放行为保持原实现；该诊断原生 30 项测试/lint 0 Error/25 Warning 通过，已纳入下述 10013 构建，真机验证待执行。

提交 `03538121eab835e2d1cc2296a65001298c983fbd` 的 ARM64 internal release `1.0.0+10013` 已构建（16915381 字节），APK SHA-256 为 `11aa3c2f6906065d454e7540467a5e566aef3e13cc163f22864fe63bf8a9e5cf`。相同独立内测签名、debuggable=false、公共 TLS 证书逐字节匹配及四变体 Manifest 检查通过；未含私钥/Keystore。等待上述 10011 隐藏补测结束后覆盖升级，不在测量途中替换 APK。10012 保留为未安装的中间构建，不作为实际视频首帧输入。

10013 已在隐藏补测结束后成功覆盖升级。红米系统视频预览实际 rendering/playing=true，但 MediaPlayer 提供的 frames/droppedFrames 固定为 0，decodeErrors 缺失；这些值不能证明实际零帧、零掉帧或零解码错误。改用当前包 UID、视频服务父层及非空可见缓冲区共同定位的自有 SurfaceFlinger 子层，读取实际呈现时间；首轮 126 个有效时间戳窗口约 29.908fps。时间戳语义依据 [AOSP FrameTracker](https://android.googlesource.com/platform/frameworks/native/+/dc3d6af97d521678981c773ad9f4e1da088d7870/services/surfaceflinger/FrameTracker.h)。只读该子层的 latency，不清空全局统计，不保存其他应用的层数据；这只是短时预检，固定长时视频报告仍待。

静态未拥有作品正常进入受限试用，实际“剩余 1:58”，随后执行整机重启。用户解锁并开启无线调试后，按原设备实例自动发现新端口并连接，无需重配；BOOT_COUNT 350→351。正常 main 没有恢复旧试用，“我的”真实读出原视频/4D 两张权益，后台同一安装凭据、权益数 2、usedQuota=2/3；重新开始静态试用仍从“剩余 1:58”开始，正常结束。返回桌面后同一 VideoWallpaperService resourceLoaded/rendering/playing/visible=true，自有呈现时间函数有效窗口约 29.915fps。该项验证实际活跃试用跨重启放弃及正式视频恢复；未读取 release 私有目录，不将源码推断的临时目录清理记为直接文件实测。

10013 的静态系统壁纸基线已完成固定 5 分钟预热+5 分钟测量：当前系统组件保持 MIUI ImageWallpaper，前台保持桌面；测量段本应用 PSS 90114～90181KiB，AC 充电/100% 电量/亮度 14 手动，温度 34.5→34.2℃。屏幕超时已恢复 300000ms。该样本是本应用进程的比较基线，静态渲染在系统独立服务中，不能把缓存 App 的 PSS/CPU 当成系统静态总成本；cpuinfo 为采样窗口统计，不能解释成瞬时功耗。首次基线因桌面焦点尚未就绪而无样本退出，保留未完成报告；补上真实桌面就绪前置检查后完成本次完整基线，未改播放代码或抹去首次失败。

正常 main→我的→“清理未使用的本地资源”显示“已清理 0.0 MB，正在使用的资源已保留”；返回桌面后当前 VideoWallpaperService resourceLoaded/rendering/playing=true，自有实际呈现时间约 30fps，后台原设备仍为三张权益。界面以 MB 四舍五入显示，本次不声称实际删除了非零大小文件；在用视频保留/继续播放成立，未读 release 私有目录。此前低规格真机的未用夹具清理及多引用保护分别见 A07 和原生测试。

10013 只针对独立内测包 user 0 执行真实 pm clear，正常“加载我的权益”建立新 Keystore 凭据且权益为 0；再 pm uninstall --user 0（没有 -k），确认该用户包列表为空后重装同一 10013 APK。首次 streamed install 被 MIUI 安装门禁拒绝，没有出现可处理的提示；改为非流式普通安装，捕获显示本内测包名称的 AdbInstallActivity，正常点击“继续安装”后 Success，没有修改系统安全开关。新装正常首页/我的及真实身份请求通过，原安装、清数据后、重装后的 credentialKeyId 三者不同；新装权益 0，原 internal 设备与既有 local 设备的后台权益仍各为 3。仅证明 user 0 卸载/重装，不宣称其他用户全局 APK 已删除，也不自动复制旧权益；未读取 release 私有凭据。

## 后续门禁

完整候选内测新装、三类业务/系统设置、不可变资源更新、断网/取消/失败保留旧版、下线权益保留、多设备并发、未知兑换恢复、正常缓存清理、活跃试用重启放弃和升级/卸载身份边界逐项补齐。内存和存储压力专项按用户要求排除；现有自动异常测试作为补充，不冒充新增真机实测。

按 A07/A08 预先固定的条件，视频与 4D 各可见预热 5 分钟、测量 30 分钟、隐藏 10 分钟；记录首帧、隐藏释放、PSS 增长、实际帧率/CPU/温度/电量和静态基线，无 ANR/崩溃。手机充电状态下不能据电量百分比宣称放电功耗合格；明确记录实际条件。

详细截图、原生状态、媒体、APK、日志及私有配置只在忽略的 `.runtime/android-evidence`、`.runtime/android-api12`、`.runtime/android-internal`，不进入 Git。A10/A11 尚未完成，未交接鸿蒙或提前公开首发。

## 最新范围调整

用户明确先完成 Android 1.0，华为/荣耀适配 2.0、苹果 3.0；隐藏试用入口并补详情真实 4D/MP4。视频可选固定测量已停止，原报告 completed=false（5 分钟预热完成，可见约 13 分钟；无 30+10 分钟通过声明），屏幕超时恢复 300000ms。不追加固定时长、内存/存储压力或机型扩测；最终包只补两类真实交互与普通进程恢复必要验证。

## Android 1.0 最新交互确认（10014/10015）

10014 internal release 已在红米 user 0 同签名覆盖安装。MP4 详情原生首帧就绪，跨越 10 秒源视频时长后保持循环播放，预览 ROI 平均像素差 3.38；4D 详情直接使用真实分层渲染，原生拖动使黄圆位移约 37.5 像素，用户此前已确认物理倾斜“有动”。两类详情均无试用入口，教程同步真实效果说明。

视频系统设置已返回系统确认。退出 App 到桌面后，仅对本包 user 0 的 PID 执行一次受控进程终止（am crash，不使用包级 force-stop），PID 22919→23719，系统宿主在约 2.6 秒自动重新绑定，playing/rendering=true、failed=false。此为主动注入的终止验证，不计为产品自行崩溃。4D 系统共用桌面与锁屏及对应进程恢复继续确认。

10015 仅修正动态目标文案和 4D 系统设置按钮，原生渲染/播放器/存储/恢复实现与上述包一致。Flutter analyze 无问题，相关详情/隐藏入口回归 9 项通过。10014 原生 30 项及 lint 0 错误/26 警告、Flutter 全量 38 项通过/1 个默认本地集成跳过；四变体 Manifest 校验通过。API 与 H5/后台未改，不重复执行既有已通过的业务全量回归。

最新验收结果：按最新 1.0 范围完成两类详情/系统设置/进程恢复、10015 升级及权益保留；前序安全业务及运行证据保留，取消的压力/额外时长/扩测不记通过，见 A10。具体版本/数字见 A10 最终记录。只对本包 user 0 主动注入进程终止，两类均由系统自动重新绑定；不是包级 force-stop，也不计为产品自行崩溃。

## 最终 1.0 收口结果

4D PID 23719→24313，2.65 秒内系统自动恢复：rendering=true、sensorRegistered=true、frames=32、failed=false；MP4 PID 22919→23719，2.64 秒内自动恢复 playing/rendering=true、failed=false。只主动终止本包 user 0 进程，两次间隔超过一分钟，不使用包级 force-stop、不记为产品自行崩溃。

最终系统服务 ParallaxWallpaperService，System wallpaper id=46，Lock wallpaper state 为空，沿用桌面共享资源；Fallback id=1 为系统兜底，不能误读为独立锁屏壁纸。系统选择确认及旧共用路线证据保留，没有独立动态锁屏入口。

10015（源码 38be91019cea1624ead14a99e94859f23049ca57）签名覆盖安装成功，4D/MP4 详情首帧确认，系统目标显示“以系统选项为准”、4D 按钮为“打开系统4D壁纸设置”。原生实现与 10014 交互/恢复验证相同，只调整设置文案。候选安装与既有 .local 均保留 3 权益；没有清数据/卸载 .local。10013 新装基线与 10015 最终升级分别记录，不声称新版本重复新装。

最终包/签名/测试/标签信息见 A11 与 OPS-003。A00–A11 按最新 1.0 范围均已完成，额外固定测量/耗尽/机型扩测不追加、不记通过。API/H5/后台本轮未改，复用前序已通过的回归。最后实际体验由用户进行，出现壁纸丢失或内存问题再排查。
