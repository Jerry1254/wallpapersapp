# WP-UI01 App UI 规范验收

日期：2026-09-14；状态：已完成，待用户最终视觉验收；版本：1.0.0+10016；用户范围：先参考 H5 建立 App UI 规范，并在菜单 Tab 加入规范页。

## 实现

- App 新增第三个「UI 规范」Tab，首次访问时创建规范页，后续切换保留状态和滚动位置。
- H5 共用 Token 直接进入 Flutter 固定主题，提供 H5 对照按钮、标签、分类、卡片、状态、导航、教程与面板样例。
- 规范页对照 H5 的视觉原则、基础 Token、页面骨架、业务组件、使用边界；演示只修改本地 UI 状态，面板显示「组件演示」。
- 示例图片复制 H5 SVG，图标提取 H5 锁定 Lucide 1.39.0 并携带完整许可；未增加业务素材或文案。
- 本轮没有更改原生 4D / MP4 能力、资源、API 或权益；试用继续隐藏。业务页整体内容同步 WP-UI02 尚未开始。

实现规范见 [APP-UI-001](../../design-system/倾境壁纸/APP-UI-001-AppUI规范.md)，状态见 [PM-002](../10-项目管理/PM-002-数据库API与H5联调开发计划.md#14-app-样式与内容调整)。

## 核对

| 项目 | 结果 |
|---|---|
| Flutter analyze | 0 问题 |
| Flutter test | 42 通过，1 个原有本地集成默认跳过；覆盖三 Tab 切换、首页不重复加载、375/390/430 滚动、1.3 字体缩放、兑换错误与设置状态返回 |
| Flutter 锁定依赖 | `pub get --offline --enforce-lockfile` 通过；SVG 渲染版本 2.3.0 |
| H5 Token 生成物检查 | `generate-design-tokens.mjs --check` 通过，生成物未改动 |
| H5 素材与 Lucide 几何检查 | `sync-mobile-ui-reference.mjs --check` 通过，保留原许可 |
| Android arm64 internal release | 构建通过，versionName 1.0.0 / versionCode 10016，minSdk 26 / targetSdk 36 |
| Manifest 策略 | 四变体现存合并 Manifest 检查通过；本轮重新构建 internalRelease，其他变体沿用已验证产物 |
| 签名 | 与 10015 相同 internal 证书，同签名覆盖安装成功 |
| 红米页面 | 菜单三 Tab 可切换；规范页、颜色、间距、分类、双列 SVG、按钮和设置面板已截图视觉核对，无溢出；返回规范页保留滚动位置 |

本步骤只做与 UI 改动相关的必要核对，没有重复 Android 1.0 长时、重启或内存专项。H5 / API / 原生逻辑未改动，既有能力验收引用 A00–A11。

## 手机与构建产物

红米 M2012K11AC / Android 13 / user 0，通过无线 ADB 在原 `.internal` 包上升级，保留设备身份和权益。启动首页与我的页面正常，查看规范组件没有发起实际兑换或壁纸设置。

- APK：`.runtime/android-candidate/qingjing-android-internal-1.0.0-10016-arm64.apk`，17,469,289 字节。
- SHA-256：`3a31eaa2cf6843cb5833032743801af60c64952337c69ae8dec2fa6279f235d6`。
- 签名证书 SHA-256：`f46b13574dc73b8bba32d008d4967306edf6d06181b174569ba94b0b7709bfa8`。
- 构建/测试日志与自有 App 截图：`.runtime/android-evidence/ui01-*`。截图覆盖规范首页、Token、页面骨架、卡片、按钮、设置演示面板与 Tab 返回；运行时文件不进入 Git。
- 构建源为 Android 1.0 文档基线 `a13c240` 后本次 UI01 改动；打包后只补验收文档，未再更改 App 源码。

## 结果

WP-UI01 已完成，红米已停在「UI 规范」组件页，用户可以直接查看。规范文档、组件、页面、构建与必要检查齐备；最终视觉由用户验收。WP-UI02 的首页 / 我的 / 详情业务内容与布局同步未开始。
