# WP-UI02 App 全站 H5 同步验收

日期：2026-09-15；状态：已完成，待用户最终视觉与真实体验验收；版本：1.0.0+10017。

## 范围

本轮以现有 H5 页面、组件和共用 Token 为内容及样式依据，整理 Android App 首页、分类、搜索、详情、兑换、下载、设置、我的、客服和教程。删除旧 App 自行增加的平台、资源、效果选择、能力说明、独立预览按钮等详情内容。

用户已经确认的 Android 1.0 原生能力继续使用：4D 详情真实交互、MP4 详情循环播放、真实兑换和安全下载、系统壁纸设置。试用入口继续隐藏；华为/荣耀 2.0 和苹果 3.0 不在本轮范围。

## 页面与交互对照

| 页面 / 组件 | App 结果 | 保留的真实能力或差异 |
|---|---|---|
| 首页 | H5 品牌栏、搜索、五列分类、筛选和双列壁纸卡；卡片只显示类型与名称 | 目录、搜索、分类和分页继续请求真实 API；App 菜单保留用户要求的第三个「UI 规范」Tab |
| 分类 / 搜索 | H5 页面栏、客服入口、搜索、筛选、状态与双列列表；保留底部 Tab | 返回和 Tab 切换使用 App 路由，数据来自真实 API |
| 详情 | 返回、标题、观看教程、1:2 真实主预览和一个橙色主操作 | 4D 使用真实倾斜/触摸交互，MP4 使用真实循环播放器；主操作进入真实兑换、下载或系统设置 |
| 兑换 / 下载 / 设置 | 使用 H5 底部面板结构、状态、按钮和文案层级 | 状态来自真实 API、签名资源下载和手机系统；动态目标位置由系统选择时只显示该真实入口 |
| 我的 | H5 顺序：品牌栏、教程、已获得壁纸、客服；列表使用 H5 行组件 | 权益属于 App 当前安装身份，因此设备说明中的“浏览器”改为“App” |
| 客服 | H5 客服卡、二维码预览、保存、微信号复制和服务时间 | 二维码由同一文案生成；Android 10 及以上保存到系统图片库 |
| 教程 | H5 五组壁纸设计内容和设置教程播放器 | 设置视频使用 H5 同一 MP4，支持播放、暂停、拖动和沉浸显示 |

页面没有加入价格、下载数、平台标签、文件大小、额外介绍或版权说明。H5 中用于浏览器联调的模拟授权、模拟下载和“浏览器联调环境”文案不进入 App；对应位置由已完成的 Android 原生流程接管。

## 自动检查与构建

| 项目 | 结果 |
|---|---|
| Flutter analyze | 0 问题 |
| Flutter test | 42 通过，1 个本地 API 条件测试默认跳过；更新首页/Tab、详情精简及试用隐藏断言 |
| Android 原生插件 | `testDebugUnitTest` 和 `lintDebug` 通过；覆盖既有真实预览、资源交付与系统设置回归，二维码保存代码通过编译和 lint |
| H5 Token | `npm run tokens:check` 通过 |
| H5 参考资源 | `sync-mobile-ui-reference.mjs --check` 通过；设置教程、二维码和 Lucide 来自锁定的 H5 依赖与素材 |
| Manifest 策略 | localDebug、localRelease、prodRelease、internalRelease 四个变体检查通过 |
| Android arm64 internal release | 构建通过，versionName 1.0.0 / versionCode 10017；APK 18,505,993 字节 |
| 签名 | APK v2 验证通过；internal 证书 SHA-256 为 `f46b13574dc73b8bba32d008d4967306edf6d06181b174569ba94b0b7709bfa8` |

APK 位于 `.runtime/android-candidate/qingjing-android-internal-1.0.0-10017-arm64.apk`，SHA-256 为 `423bd1f28792dd7e684ef98341a2343e3a7eb24fdc7214d92de3e30ef5f16051`。构建日志、APK 和自有 App 截图均位于 `.runtime/`，不进入 Git。

## 红米短核对

红米 M2012K11AC / Android 13 已重新配对无线 ADB，通过同签名覆盖安装 10017，原安装身份和三项权益保留。电脑恢复隔离 API 8083、loopback HTTPS 8443 和 ADB reverse 后，电脑与手机的公开分类请求均返回 HTTP 200。

- 首页：品牌栏、搜索、分类、筛选、双列真实目录和三个 Tab 正常。首次核对发现 Flutter `maxLength` 自动显示 H5 没有的“0/40”，改用输入限制器后重新构建、覆盖安装并确认计数器消失。
- 详情：4D 和 MP4 均使用 1:2 真实预览，只显示观看教程和单一“设置壁纸”主操作；旧平台、资源、能力说明和独立预览入口均未出现。4D 画面可交互；MP4 间隔一秒的媒体区域截图摘要不同，真实播放器在推进。
- 教程：H5 设置视频显示正常，可从 00:00 播放到 00:05，播放、进度和全屏控件可用。
- 我的与客服：三项既有权益正常读取；页面顺序、壁纸行、二维码、微信号和服务时间与 H5 一致。二维码大图预览正常，保存按钮在真机返回“二维码已保存，请检查相册”。

本轮按用户要求只做页面与交互短核对，没有重复 Android 1.0 的长时、内存、重启或压力测试。最终视觉和日常真实体验由用户继续验收。
