# 管理后台原型验收记录（2026-09-10）

**工程：** `apps/admin-web`
**状态：** 自动检查通过，待人工验收

## 自动检查

| 检查 | 结果 |
|---|---|
| `npm run typecheck` | 通过 |
| `npm run build` | 通过 |
| 登录错误提示 | 通过，错误密码显示“账号或密码错误” |
| 管理员登录 | 通过，进入工作台并建立会话 |
| 工作台 | 通过，统计、最近更新和快捷入口正常 |
| 壁纸列表 | 通过，类型、平台、资源完整度和状态显示正常 |
| 新建 4D | 通过，固定 Android，显示封面、背景层、透明前景层和景深配置；不要求独立预览视频 |
| 新建动态 | 通过，勾选 iOS 后动态增加 MOV 与 HEIC 必填资源 |
| 新建静态 | 通过，只显示封面和高清原图资源 |
| 空表单发布 | 通过，显示名称、分类、封面和类型资源错误 |
| 编辑现有壁纸 | 通过，正确回填基础信息、资源和预览 |
| 保存并发布 | 通过，列表更新时间刷新并显示成功提示 |
| 生成兑换码 | 通过，生成 3 个兑换码、额度 3，列表新增 `0 / 9` 批次 |
| 完整码保护 | 通过，关闭结果并刷新后完整码不再出现在页面 |
| 分类树 | 通过，一级分类展开显示其二级分类 |
| 分类节点联动 | 通过，选择一级分类节点时显示图标上传，选择二级节点时隐藏图标上传 |
| 分类图标上传 | 通过，选择本地 SVG 后立即显示预览 |
| 壁纸分类联动 | 通过，选择“风景”后只显示自然风光、城市夜景、海洋天空 |
| 兑换码状态移除 | 通过，领域模型与批次表格均无启用/停用状态 |
| 壁纸删除保护 | 通过，列表提供删除操作并在执行前显示壁纸名称和不可恢复提示 |

## 浏览器验证环境

- 本次 5176 端口已被考试管理后台占用，倾境后台由 Vite 自动运行在 `http://127.0.0.1:5177`。
- 工程默认端口仍为 5176；独立启动且端口空闲时使用 `http://127.0.0.1:5176`。
- 验收数据存储在浏览器 LocalStorage，仅用于 Mock 演示。

## 人工验收项

1. 上传抽屉的信息密度、字段顺序和运营文案。
2. 三类资源与实际素材生产流程是否一致。
3. 表格字段、筛选项和管理动作是否需要增减。
4. 兑换码导出格式是否适合客服发码流程。
