spring:
  flyway:
    enabled: true
    clean-disabled: true
  main:
    banner-mode: off

logging:
  level:
    root: WARN
    org.flywaydb: INFO

qingjing:
  storage:
    root: ${java.io.tmpdir}/qingjing-wallpaper-integration-storage
